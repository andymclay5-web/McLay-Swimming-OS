McLay Swimming OS Version 4
Build: v4-poolside-core-20260819f-targettruth
Status: SOFTWARE GUARDIAN PASS — PHYSICAL PHONE RELEASE GATE STILL REQUIRED

Complete V4 Guardian
- 82 / 82 foundation and Version 4 regressions PASS on the shipping source stack:
  app.js + v4-correct.js + v4-poolside-core.js.
- The full Guardian is now executed by GitHub Actions on changes to the V4 runtime.
- Morning Board session/coaching regressions remain PASS.
- JavaScript syntax checks pass for the main app, V4 layers, Morning Board, engines and service worker.

Resolved in this build
- The exact Saturday 5,450m source is now the release acceptance fixture. Its 1,100 / 850 / 2,900 / 600 section truth, target-driven lines and known 6,090m saved-state repair are checked together.
- Historical swimmer `legacy_pace` T400 references migrate into Version 4 Freestyle/SCM test evidence, so the seven attending National swimmers immediately show their existing anchors in Times and Board targets.
- Standalone authored ranges now retain their target meaning: `#2-6 @ 100m Race Pace`, `#1-4 Overload`, `#5-8 Threshold`, `#2-4 @ 100m Race Pace`, and `Second 100 of 200 Race` each expose a compact target dropdown or an explicit target-needed state.
- Modified mixed-zone work keeps every authored phase. The Saturday 2 × 400 remains two phases for Charlotte (2 × 200) and McKenzie (2 × 275), with separate Regeneration and Development targets rather than collapsing to one repeat.
- Target dropdown rows refresh when opened, so asynchronously hydrated T400/PB evidence appears without rebuilding the whole Board or hiding the parent set.
- Manual T400 initializes and updates the settings object belonging to the state being saved, so a locally entered T400 immediately becomes the swimmer anchor and keeps that swimmer in the session timing roster.
- 400 IM race intent remains separate from the working stroke. A Fly repeat at 400 IM pace does not invent a straight-line IM-leg target when no exact IM leg model is loaded.
- Clearing an explicit athlete override resumes the current automatic athlete profile. Charlotte returns to the current 50% profile rather than the obsolete 60% expectation.
- App, V4 layer and service-worker cache identifiers are advanced together so the corrected bytes replace the previous phone cache.
- The final poolside build now owns the software attestation used by the release gate; an earlier layer can no longer leave the shipping build marked DEV / NOT ATTESTED.
- Startup now activates the saved view before rendering, so reloading or reopening on Times, Roll, Coach Hub, Guardian or another non-Board screen cannot leave the main area blank.
- Standalone `10s rest` / `30s rest` lines can no longer be parsed as 10m / 30m swimming items.
- The exact saved Saturday corruption seen on the phone — 6,090m with 1,700 / 850 / 2,940 / 600 sections — is repaired locally and non-destructively to 5,450m with 1,100 / 850 / 2,900 / 600. The duplicate 4-round breakdown is folded into the parent 12 × 50 and attendance, captures, source evidence, user edits and finish notes remain intact.
- Existing T400 evidence is merged even when Version 4 sessions already exist. Historical generic/name-labelled T400 records resolve immediately, and T400 course defaults to SCM only when the source has no explicit course.
- Board target detail is collapsed into a right-aligned parent-set dropdown. Opening it keeps the complete set visible and shows swimmer-grouped targets/send-offs without inline desktop clutter.
- Board now has direct Swimmers / Pathway access. The swimmer hub answers PB → next manageable step → national gap in seconds and percentage, then opens the attendance-linked recent training exposure for that event/stroke.

Retained V4 scope
- Full legacy app and data paths remain present; this is not a seven-file replacement shell.
- Board, Coach Hub, Roll, Times, Note, Voice, Video, Edit and Finish remain loaded.
- Voice transcription remains in the v4-correct transcript build and local-first capture path.
- TV Board, Individual Device, Assistant Coach and Meet Deck remain first-class V4 surfaces.
- Morning Board evidence retrieval, McKenzie adaptation rules and canonical session-truth engines remain included.

Release boundary
- Production cloud writes remain Guardian-locked.
- Physical Android/PWA acceptance, microphone/camera permission behaviour, authenticated Supabase reads/writes, installed-service-worker transition and real poolside stability still require live-device proof before this build is described as fully cleared for a coaching session.
