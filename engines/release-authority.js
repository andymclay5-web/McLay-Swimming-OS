'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-spine-audit-bridge-fix-20260921';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260921-spine-audit-bridge-fix';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-21T09:30:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Engine-audit/spine review, 21 Sept 2026, carried out immediately after the repo-drift restore above shipped (Andy: \'let\'s start with this engine audit and spine thing... make sure the engines are operating as intended and that they are not having interference with each other and then go back through the pipeline\'). Audited the whole session-input -> parse -> Board-delivery -> reporting spine against this project\'s own documented ownership map (architecture/AUTHORITY_MAP.md, WRITER_MAP_FINDINGS.md, and a third, more recent doc found in the process, architecture/RUNTIME_AUDIT_20260909.md, which supersedes several of the first two\'s findings). Six previously-flagged gaps were checked one by one against current code: attendance-roster.js\'s addSquad illegal session write (confirmed FIXED, 4 Sept); live-training-authority.js\'s L.apply missing a pre-apply revision check (confirmed FIXED, 4 Sept); meet-ops-av.js\'s parallel localStorage backup channel (still open, Meet-domain, low risk, left alone as Meet is shelved); storage.js\'s unawaited hydrate() race (still open, mitigated but not eliminated by startup-gate.js gating first paint on S.ready/S.readyPromise -- a real but narrow window remains, not touched this pass); stability-identity-bh.js\'s A.role() background-force-navigate landmine (confirmed genuinely unreachable today -- access-authority.js loads after it and unconditionally overwrites A.role/A.setRole -- but one script-load-order change away from resurfacing, left as dead code rather than risk reordering load-order-sensitive files without a concrete reason); and the \'un-retired transitional wrappers\' item, confirmed to mean only v4-correct.js/v4-poolside-core.js are still actually shipped (a handful of older wrapper files exist in the repo but were never added to index.html and stay genuinely dead). Independently of that list, the review also verified the 9 Sept RUNTIME_AUDIT\'s own fix -- routing the live Board/Times-panel target display through Coordinator.prescription() (which correctly adapts an item for a modified swimmer first) rather than the older Coordinator.targetForItem() (which never adapted) -- is still correctly in place and holding.\n\nThat same check surfaced one real boundary violation the 9 Sept fix itself had not caught: engines/coordinator.js\'s prescription() -- the exact function the 9 Sept fix made authoritative for the live Board -- called E.Modification.adaptItem() directly instead of through engines/bridge.js\'s sanctioned M.adapt.item wrapper, the ONE path every other adaptItem() caller in the app (app.js\'s mod-edit modal, v4-correct.js\'s lane grouping) already goes through. That wrapper\'s preserveRepeatingDescent() exists specifically to restore a coach-authored \'Desc 1-3\'-style descending-pattern cue that adaptItem\'s own condensed-reps regeneration can otherwise lose or relabel against the swimmer\'s new rep count. Net effect before this fix: a modified swimmer\'s descending-set cue could show correctly in the mod-edit modal (bridge-routed) but wrong or regenerated on the live Board for the exact same item and athlete -- two different answers for the same swimmer\'s same set on two parts of the same screen. Fixed: prescription() now calls the real bridge wrapper (root.MSOS4?.adapt?.item) when it has loaded, falling back to a direct adaptItem() call only when it hasn\'t (several existing tests exercise coordinator.js standalone without loading bridge.js at all; that fallback reproduces this function\'s exact pre-fix behaviour for them, unchanged). Regression: new tests/prescription-bridge-descent-preservation-20260921.cjs -- boots the real coordinator.js against the real bridge.js in an isolated sandbox with a controlled fake Modification engine simulating a genuine 6-to-2 rep condensation, proves the live-Board path now preserves the authored \'Desc 1-3\' cue instead of the wrongly-regenerated \'Desc 1-2\' one; a fail-before pass against the exact reverted pre-fix source reproduces the real bug, confirming this test would have caught it; a third check confirms the two existing tests that already exercise coordinator.js without a loaded bridge (modified-target-authority-20260909.cjs, board-mod-reason-not-on-board-20260913.cjs) keep passing unchanged. Full sweep: same standing baseline, zero new regressions, plus this build\'s own new test passing. **Overall spine verdict, stated plainly**: engine separation across the session-input/parse/Board-delivery/reporting spine is fundamentally sound and has held up well through three weeks of continued development -- no engine outside the parse group was found mutating canonical session structure directly, and no spine engine was found writing board-state.js\'s or modification.js\'s owned state directly, both clean. The one real, live, user-visible issue found (the cue-preservation bridge-bypass above) is now fixed and tested. Two lower-risk items remain open and are being left alone deliberately rather than risk touching them without a concrete forcing reason: the storage.js hydrate() race (mitigated, narrow residual window) and the dormant stability-identity-bh.js dead code (confirmed unreachable by current load order).\n\nPrior note (repo-drift-restore build, superseded by the above -- kept verbatim for the history it documents): Andy, live, 21 Sept 2026, right after the full-payload-safe-enable build below shipped and its own regression tests all passed clean: while starting the separately-requested \'engine audit and spine\' review (checking that the session-input -> parse -> Board-delivery -> reporting pipeline is solid and that engines are not reaching across boundaries into each other), a routine `git fetch origin` sync step surfaced something far more serious than the audit itself -- origin/main, the actual live repository this app is served from, had silently reverted to old content across 19 files, independently of and unrelated to today\'s own delivery (which remained correctly and fully applied throughout). Confirmed by diffing local HEAD content against origin/main file-by-file, then cross-referencing every differing file\'s exact byte content against this repository\'s own full commit history (`git show <commit>:<file> | sha256sum` against every commit touching that file) to find an EXACT match -- proving, not assuming, that all 19 were pure, clean reversions to specific old commits already sitting in this project\'s own git history, never foreign or mixed content that would need careful manual reconciliation. Seven source files had lost real, previously shipped and Andy-confirmed fixes: swimmer-portal.js/swimmer-portal.css (the calendar session-picker -- sessionsList()/SELECTED_SESSION_ID -- and its .sp-session-block/.sp-session-item styling); engines/swimmer-invite-bn.css (the QR modal max-height:88vh;overflow:auto scroll fix from the qr-modal-scroll-fix build, without which the access modal locks solid again exactly as it did the night that fix shipped); engines/swimmer-instant-open-cn.js and engines/swimmer-training-bd.js (the Charlotte Murphy WPS/WA points mislabeling fix and its companion \'0 WPS\' null-check); engines/t400-capture.js (the 11 Sept bulk-T400-entry meta?.silent fix); v4-poolside-core.js (the 13 Sept \'IM\' autocorrect fix). Alongside these, eight regression test files that proved those exact fixes had been deleted outright (tests/bulk-t400-entry-20260911.cjs, im-autocorrect-typo-20260913.cjs, para-scoring-wa-mislabel-20260918.cjs, qr-evidence-pathway-standards-pagesize-20260918.cjs, qr-modal-scroll-overflow-20260920.cjs, qr-session-save-auto-publish-20260920.cjs, session-methodology-20260911.cjs, task64-reference-sync-20260911.cjs), three more test files had been edited back to weaker or older assertions that would no longer have caught the fixes they were reverted alongside (board-race-simulation-fins-quality-20260918.cjs stripped of its own PRE_FIX_COMMIT pinning back to reading HEAD directly; swimmer-evidence-progress-callback-20260910.cjs reverted to expecting 6 progress-callback calls instead of 9, missing three post-loop checkpoint assertions; swimmer-surface-co.cjs reverted to matching an old inline literal), and one architecture doc had been deleted (architecture/RUNTIME_AUDIT_20260909.md). This was reported to Andy factually and completely, without speculating on how it happened, before touching anything -- and Andy responded plainly: \'Fuck, mate, I\'ve been following your instructions the whole time. So, yeah, of course I want it fixed,\' explicit authorization to restore everything. Because every one of the 19 files was proven to be a clean reversion to content this project\'s own history already holds -- not new work -- this build is a REDELIVERY of already-written, already-tested, already-Andy-confirmed code and tests back to their correct current state, not a new fix with new logic or new risk. **Root cause stated plainly: unknown.** This app is deployed by Andy manually uploading each delivery\'s zip contents through GitHub\'s web UI rather than through a git merge or push, which means the repository\'s commit graph cannot by itself prove which upload happened when or whether an earlier upload\'s content was ever fully or partly re-applied out of order -- a real fragility in that workflow, worth a direct conversation, and directly relevant to the \'spine\' reliability review this discovery interrupted. This build does not claim to know whether it can recur, and the engine-audit/spine review itself -- checking the session-input -> parse -> Board-delivery -> reporting pipeline against the engine-separation map, Andy\'s actual original ask -- has not yet resumed. Full sweep after restoring all 19 files plus this build\'s own version bump: same standing baseline failure set as this environment\'s own true baseline, confirmed unchanged by diffing the full failing-file list before and after, zero new regressions.\n\nPrior note (full-payload-safe-enable build, superseded by the above -- kept verbatim for the history it documents): Andy, live, 21 Sept 2026, right after the session-auto-publish build below shipped and Matthew Robertson\'s access kept working end to end: "the whole point is that they can see their information, their history and actually use it as a tool for training and racing... we need to get them to be able to see all of the information I have for them." That full analytical payload (performance pathway ladders, training accumulation, test history, meet history) had been deliberately disabled since the very first night it shipped, after payloadFor() locked William Callow\'s phone solid for a later Matthew Robertson attempt -- disabled rather than guessed at again under pressure, on record as its own open item ever since. Re-enabling it safely meant actually finding and fixing what could make it slow, not just moving where it ran. Investigated properly this time: engines/performance-pathway-ck.js\'s buildAthletePathways() was directly profiled (not guessed) with a real Node harness against a synthetic pathway_standards table sized to Andy\'s own real, repeatedly-documented ~4,406-row figure -- confirmed a genuine O(events x standards-table-size) inefficiency (buildEventLadder() rescanned the ENTIRE reference table from scratch, unfiltered, for every single ranked event) and fixed it by indexing pathway_standards/pathway_meets once per athlete (buildStandardsIndex/buildMeetsIndex, grouped by exact distance+stroke / programme family) instead, reused across every event -- O(standards + events) instead of O(events x standards), a measured 7x speedup in this sandbox (2.3x during initial development profiling; both runs verified against the real pre-fix file via git, not a hand-copied approximation) at Andy\'s realistic event count, with byte-for-byte identical output (same active/sex/para filters, same grouping, same ordering -- only where the standards/meets rows come from changed; see tests/performance-pathway-ck-index-parity-20260921.cjs for the identical-output proof at a larger, messier fixture than any prior test used, and tests/performance-pathway-ck-index-perf-20260921.cjs for the measured speedup as a real committed assertion, not just a one-off profiling script). Independently of whether the standards-table scan was ever the WHOLE cause, engines/swimmer-invite-bn.js\'s disabled synchronous setTimeout(payloadFor) block -- the actual mechanism that froze the whole phone, since JavaScript has one thread and deferring WHEN a computation starts does nothing to stop it hogging that thread once it does start -- has been replaced with payloadForAsync: a real async generator over the same five stages (sessions/performance/training/tests/meet/shared_evidence, each still computed by the exact same unchanged safeX() functions as before -- same logic, same output shape, nothing recomputed differently), yielding back to the browser\'s event loop between every stage via a real setTimeout(0) tick, and bounded by a hard wall-clock ceiling (ENRICHMENT_BUDGET_MS, 4000ms) that skips all remaining stages if already exceeded (recorded as enrichmentTruncatedAt on the breadcrumb) rather than adding more synchronous work on top of an already-slow run. Generate\'s critical path is completely unchanged by any of this -- it still publishes only corePayloadFor\'s fast, twice-proven-bounded minimal shape; the full payload is still a second, deferred, never-awaited publish that can only add data to an access that has already fully succeeded, never delay or fail it. Regression: new tests/performance-pathway-ck-index-parity-20260921.cjs (the indexed implementation produces byte-for-byte identical output to the real pre-fix implementation, loaded straight from git HEAD, across two athletes and 60 realistic events against 469 standards rows); new tests/performance-pathway-ck-index-perf-20260921.cjs (a real, committed, warmed-up median-of-many-iterations benchmark at Andy\'s documented ~4,406-row scale, asserting a real speedup floor and an absolute wall-clock ceiling, not just printing a number); rewrote tests/qr-generate-core-then-background-enrichment-20260920.cjs (previously pinned the DISABLED state -- now proves the safe re-enablement: Generate\'s critical-path publish still carries only corePayloadFor\'s minimal shape; a second publish genuinely follows carrying the full analytical payload; a dedicated sentinel test proves payloadForAsync actually yields the thread between stages, with its own fail-before proof that removing the yield call breaks that same test; a direct test of the wall-clock budget genuinely skipping every remaining stage, not merely labelling them); updated tests/qr-generate-concurrent-attempt-guard-20260919.cjs (the winning attempt\'s own legitimate background-enrichment writes that now follow \'done\' are real, wanted behaviour -- its assertions now confirm the stale, SUPERSEDED first attempt\'s frozen step can never resurface, that the second attempt\'s own core outcome fields are never overwritten by its own enrichment, and that its enrichment genuinely completes rather than hanging). Full sweep: same standing failure set as this environment\'s own true baseline (the Playwright/local-server-dependent suite plus the one already-flagged styles.css hash mismatch), confirmed identical before and after by diffing the full failing-file list, zero new regressions, plus this build\'s own four new/rewritten test files all passing. **Still open, stated plainly**: the wall-clock budget is a real safety net, not a guarantee every swimmer\'s full payload finishes on every device every time -- a swimmer with an unusually large personal event count, or a genuinely slow phone, could still see a partial (enrichmentTruncatedAt-flagged) analytical payload on a given Generate/session-save, with the remaining sections filling in on the swimmer\'s next portal refresh once the next publish attempt has more budget headroom; this has been verified via direct profiling and a real Node test harness, not yet against Andy\'s own real device conditions live. It also does not touch the separate, already-flagged pathway_standards/pathway_meets local-sync issue (the local overlay has sat at 0 rows all session) -- once that is ever fixed, the newly-indexed lookup path should be re-profiled against a live local+reference merge rather than the reference-only case tested here.\n\nPrior note (session-auto-publish build, superseded by the above -- kept verbatim for the history it documents): Right after the qr-modal-scroll-fix build below shipped and Matthew Robertson\'s access genuinely worked end to end for the first time -- session reached his phone, he finished it, his notes came back to Andy -- Andy asked directly: "will tonight\'s session show on his phone when I write it?" The honest answer, until this build, was no. A swimmer\'s device pairing is permanent (the one-time QR exchanges for a device token saved on their own phone, proven live tonight), but the actual session DATA only ever got (re)published to Supabase from inside "Generate 15-minute QR" itself -- meaning Andy had to reopen every individual swimmer\'s own access screen and tap Generate again, every time he wrote a new session, for every swimmer who already had access. That does not scale past one or two athletes, and it is the real substance behind Andy\'s "how much longer" frustration tonight -- not a new bug so much as a piece of the original ask ("give swimmers access to their info and their squads sessions") that was never actually finished. Fixed without touching app.js at all -- Store.putSession is a checksum-protected release asset (release-package.test.js\'s `mutable` set deliberately excludes app.js) and Coach Hub/app.js stay off-limits without concrete evidence tying them to a real bug, per standing instruction. Instead engines/swimmer-invite-bn.js now wraps the EXISTING M.store.putSession the exact same optional-hook way app.js already layers cloud sync onto it (Store.putSession\'s own body already calls `M.cloud?.stageSession?.(...)` right after saving -- this is that identical pattern, from a different engine, not a new technique). Whenever ANY session is saved (new session created, session edited, or intake applied -- the three real places app.js calls Store.putSession), every currently-active athlete belonging to that session\'s squad(s) (the same case-insensitive squad match app.js\'s own UI.currentAthletes/D.presentAthletes already use) gets their swimmer-portal payload silently republished in the background, using corePayloadFor -- the same minimal, twice-proven-fast shape Generate\'s own critical path already trusts, never the full analytical payloadFor (that stays deliberately disabled). This can never block or fail the session save itself (fire-and-forget, each athlete\'s publish independently wrapped in try/catch), and is harmless to run for an athlete never given access at all -- msos_publish_swimmer_payload is a plain upsert nobody can read without their own device token, so it just sits there ready for whenever Andy does eventually generate that swimmer\'s first QR. Regression: new tests/qr-session-save-auto-publish-20260920.cjs (saving a session republishes exactly the active athletes in its squad(s) and no others -- not a different squad, not an inactive athlete in the same squad; the republished payload carries corePayloadFor\'s real session data, not an empty stub; the session save itself still returns its original result, unblocked; fail-before/pass-after against the real source with the hook removed). Full sweep: same standing 21-failure baseline, zero new regressions. **Still open, stated plainly**: this makes already-paired swimmers\' sessions current automatically from now on -- it does not yet make a NEW swimmer\'s first-ever access automatic; that still requires Andy to open their card and tap Generate once, the first time only. It also does not touch the separately-disabled full analytical payload (performance/training/tests/meet) -- that remains its own piece of work, not yet started.\n\nPrior note (qr-modal-scroll-fix build, superseded by the above -- kept verbatim for the history it documents): Right after the qr-encoder-inlined build below shipped and the QR image finally rendered live on Andy\'s phone for the very first time (confirmed twice, on Mackenzie\'s swimmer access), the WHOLE MODAL then appeared to lock solid both times: no reachable Close, Copy link barely visible. Andy, understandably furious at this point: "I just need you to be more careful when you\'re fucking around and actually look at the thing as a whole... let\'s be a little bit more decisive when we do things." Investigated holistically rather than reactively before touching anything, exactly as asked, comparing this modal against every other modal in the whole codebase rather than guessing at another timing/JS theory. Root cause found: this was never a JS freeze at all -- every click handler had been firing correctly the entire time. Every other modal in this app (app.js\'s modal(), used for session edit/intake/new-session/mod-edit) wraps its content in a `<section class="modal">`, and styles.css\'s `.modal{max-height:88vh;overflow:auto}` rule is what caps that content\'s height and makes it scroll inside the fixed, full-screen `.modal-backdrop` (`display:grid;place-items:end center`, which bottom-anchors its child with no scroll ability of its own). engines/swimmer-invite-bn.js\'s modal() is the ONLY modal in the entire codebase that never used the `.modal` class -- its content box, `.bn-access-modal`, had no height cap or overflow rule of its own. This never surfaced before tonight because the QR image had never once actually rendered across the whole multi-hour CDN saga (three CDN failures, then a fourth failure loading the replacement vendored file) -- the modal only ever showed a short placeholder line, comfortably under one screen. Tonight, for the first time, the QR encoder is fully inline and reliable, so a real ~240px canvas plus the revealed link box render every time -- pushing this modal\'s real content taller than the viewport for the first time ever, with the excess bottom-anchored off the top of the screen and no way to scroll to it. Fixed with a single CSS change, no JS touched at all, no risk whatsoever to the QR encoder itself: engines/swimmer-invite-bn.css\'s `.bn-access-modal` rule now carries the exact same `max-height:88vh;overflow:auto` every other modal in this app already relies on. Regression: new tests/qr-modal-scroll-overflow-20260920.cjs (asserts `.bn-access-modal` carries both a height cap and overflow:auto, cross-checked against styles.css\'s own `.modal` rule so both use the identical containment strategy rather than two different numbers that could drift apart; fail-before/pass-after against the exact pre-fix CSS rule, proving this test would have caught this precise regression). Full sweep: same standing 21-failure baseline, zero new regressions. **Standing lesson**: a symptom that looks exactly like a JS freeze (unresponsive buttons, no error, only a hard back-out escapes it) is not always one -- checking this modal\'s structure against the one other proven-working pattern already in the same codebase found the real cause in minutes, where a fifth guess at timing/concurrency would not have.\n\nPrior note (qr-encoder-inlined build, superseded by the above -- kept verbatim for the history it documents): Immediately after the qrcode-local-vendor build below shipped, Andy hit a FOURTH QR failure, TWICE in a row, on a brand new message: "QR renderer is missing from this build (engines/qrcode-local.js did not load)." That build correctly removed the live CDN dependency, but replaced it with a second same-origin FILE that still had to be separately deployed and separately loaded via its own <script> tag -- and every other fix tonight had only ever touched files that already existed on Andy\'s live site and updated correctly every single time. A brand-new file is exactly the one kind of change most likely to be missed by a manual deploy step, and the failure was persistent (repeated identically on retry), consistent with the file simply not being present rather than a transient blip. Rather than ask Andy to go debug his own deploy pipeline under these conditions, this build removes that risk category entirely: engines/qrcode-local.js is deleted, and its QR-encoding algorithm (qrEncode()/drawQr(), unchanged logic, re-validated after moving) now lives INLINE inside engines/swimmer-invite-bn.js itself -- the one file that has proven itself reliable on every single deploy tonight. There is no second file, no separate <script> tag, no window global, and no precache entry that could ever be missing independently -- if swimmer-invite-bn.js loaded at all (and it always has), the QR renderer is there. Re-validated after the move with the same independent method as before (OpenCV\'s QRCodeDetector decoding real invite-URL-shaped test cases back to the exact original string, at error-correction level M) to confirm nothing was lost or mistyped in transcribing the algorithm into its new home. Regression: rewrote tests/qr-generate-no-external-cdn-dependency-20260920.cjs (no more separate-file checks -- now asserts swimmer-invite-bn.js itself contains no external URL, no dynamic script-tag creation, no window.QRCode dependency of any kind, and that its own inline qrEncode() produces a structurally valid, deterministic QR matrix with correct finder-pattern placement); rewrote tests/qr-generate-qr-image-load-resilience-20260920.cjs (the "renderer missing" scenario is no longer constructible -- the encoder can\'t be missing independently of the file that\'s always been reliable -- replaced with a test that the isolation still holds for a genuine draw-time exception, e.g. a canvas API failure). Full sweep: same standing 21-failure baseline, zero new regressions. **Standing lesson**: when a fix introduces a new file as part of removing an external dependency, remember that "new file" is itself a deploy-risk category distinct from "external dependency" -- inlining into an already-reliable file removes both at once.\n\nPrior note (qrcode-local-vendor build, superseded by the above -- kept verbatim for the history it documents): Right after the qr-image-load-resilience build below shipped -- which fixed loadQr()\'s forever-cached rejection AND isolated the QR-image draw from the rest of Generate\'s outcome -- Andy hit the identical "QR renderer could not load" message a THIRD time: "Fucken useless." Crucially, this breadcrumb proved both of that build\'s fixes were working exactly as designed: outcome:\'ok\'/step:\'done\' (access genuinely succeeded, isolated correctly), and this was a genuinely fresh network attempt roughly 20 minutes after the previous one, not a replayed cached rejection. Three separate genuine failures to reach the same external host (cdn.jsdelivr.net), across three different builds, over about an hour, is exactly the pattern that points at the network path itself rather than anything left to fix client-side -- so rather than attempt a fourth retry-logic guess, this build removes the live third-party dependency entirely. engines/qrcode-local.js is a from-scratch, dependency-free implementation of the QR encoding algorithm (ISO/IEC 18004) -- byte-mode encoding, automatic version selection, Reed-Solomon error correction, full module placement (finder/timing/alignment patterns, format+version info), all 8 mask patterns scored and the best one chosen -- written and validated independently of any third-party library source. Validation: an independent decoder (OpenCV\'s QRCodeDetector, a different codebase entirely from the encoder) round-tripped 9 required test cases -- short strings, realistic 125-142-char invite URLs carrying the exact 64-hex-char token shape msos_create_swimmer_invite produces, version-capacity boundary cases, mixed-case/digit/URL-punctuation strings -- all decoding back to the exact original string at the app\'s error-correction level (M). A from-scratch Reed-Solomon-syndrome + bitstream re-parser (a second, independent check, not just re-running the encoder\'s own math) additionally confirmed 56/56 generated symbols across every QR version 1-40 and all four EC levels were spec-valid. This file loads as a plain same-origin <script> tag, in sw.js\'s REQUIRED precache list, exactly like every other engine in this app -- so window.QRCode is a real function long before a coach ever taps Generate, and there is no more live network request of any kind, to any host, anywhere in the QR-image step. loadQr() in swimmer-invite-bn.js now does nothing but check that global is present; the CDN URL, the dynamic <script> injection, and the qrPromise caching mechanism (whose own forever-cached-rejection bug was only fixed, not needed, two builds ago) are gone completely -- the entire class of bug they were built to guard against no longer has a code path to occur in. Regression: new tests/qr-generate-no-external-cdn-dependency-20260920.cjs (asserts loadQr() contains no external URL, no dynamic script-tag creation, and no network-dependent promise caching; asserts engines/qrcode-local.js is present in both index.html\'s script tags and sw.js\'s REQUIRED precache array; asserts QRCode.encode() from the vendored file produces a structurally valid, non-empty module matrix for a realistic invite-shaped URL; fail-before/pass-after against the exact pre-fix loadQr() source, proving this test would have caught the CDN dependency it replaces). Full sweep: same standing 21-failure baseline, zero new regressions. **Resolved**: the open question from the prior build -- whether the CDN failures were a genuine recurring network/CDN reachability problem rather than a fixable client bug -- is now moot regardless of the answer, since the app no longer depends on reaching that host (or any third-party host) at all for this feature.\n\nPrior note (qr-image-load-resilience build, superseded by the above -- kept verbatim for the history it documents): Right after the background-enrichment-disabled build below shipped, Andy reproduced the exact same "QR renderer could not load" message TWICE more on two separate attempts -- and reasonably was furious: "Are you fucking serious come on Claude enough of this shit." Both breadcrumbs looked identical: outcome error, lastCheckpoint load_qr, resolved in ~2.5 seconds -- fast, not a freeze, but a real, reproducible failure, not the one-off connection blip it was first assumed to be. Two genuine bugs found and fixed together: (1) by the time the QR-image load step runs, access is ALREADY fully granted -- the invite exists and the real link is already sitting in the modal, copyable -- but that step shared the exact same try/catch as the rest of Generate, so its failure painted the whole attempt red with a scary error and blanked the QR box, giving a coach no reason to trust the perfectly good link already on screen; (2) loadQr() cached its promise forever, success OR failure -- so once the CDN script load failed even once, EVERY later Generate attempt in that same page session replayed the identical stale rejection without ever touching the network again, which is exactly why Andy saw the identical message twice in a row rather than a one-off. Fixed: the QR-image draw is now isolated in its own try/catch -- on failure the attempt still reports outcome:\'ok\'/step:\'done\' (access is real), a separate qrRenderOutcome/qrRenderMessage records the cosmetic failure, the url box and Copy link stay visible and correct, and the status text says the link is ready with a plain note that only the image failed, instead of reading like nothing worked; loadQr() now clears its own cached promise on failure so every Generate attempt gets a genuine retry against the network rather than replaying a stale result. Regression: new tests/qr-generate-qr-image-load-resilience-20260920.cjs (a QR-image failure leaves outcome/step at ok/done with the real link still visible and usable; a second attempt in the same session genuinely re-touches the network and can succeed after the first failed; fail-before/pass-after on the exact loadQr() source change, proving a second attempt on the pre-fix source replays the same stale rejection without a new script load at all). Full sweep: same standing 21-failure baseline, zero new regressions, 133 passing. **Still open**: whether the underlying CDN load is failing for a genuine, recurring reason on Andy\'s network/device (a captive-portal quirk, an ad-blocker, DNS) rather than pure chance -- worth asking him directly if it recurs a third time even with the retry fix in place, since that would point at the network path itself rather than anything client-side left to fix.\n\nPrior note (background-enrichment-disabled build, superseded by the above -- kept verbatim for the history it documents): A few hours after the core-payload-background-enrichment build below shipped -- and right after it reached William Callow\'s "payload:performance" step cleanly in 2.5 seconds, proving the critical-path half genuinely worked -- Andy hit a DIFFERENT, WORSE symptom on a later attempt for Matthew Robertson: "I can\'t copy the link cause it\'s frozen, cant do anything but back back." Not one modal stuck like all three earlier freezes tonight (those always left the rest of the UI responsive) -- the WHOLE PHONE, unresponsive to every tap, forcing a hard back-out. That build\'s own design comment argued that deferring the full analytical payloadFor() call via setTimeout(...,0), outside GENERATE_TIMEOUT_MS, meant it "could no longer delay or fail the access the coach already has in hand." That was right about the access staying safe, but WRONG about the phone staying usable: JavaScript has exactly one thread, so deferring WHEN a synchronous computation starts does nothing to stop it from hogging that one thread for however long it takes once it actually starts running -- a still-running background republish blocks every tap on the page exactly as badly as a blocking foreground one would, just later and disconnected from whichever button was last pressed, which is even more confusing to diagnose from the coach\'s side. safePerformance()/buildAthletePathways() (performance-pathway-ck.js, race-pace.js, wa-points.js) was read carefully earlier the same night hunting for the THIRD freeze and no unbounded loop was found -- but that is the exact same "read it, find nothing" result that missed both of the other two real bugs tonight before they were finally caught by counting real operations against real data volume instead of just reading code. Rather than ship a fourth guess while a coach\'s phone was actively locked up mid-session, the deferred background-enrichment republish is disabled entirely for now (engines/swimmer-invite-bn.js: the setTimeout block is commented out in place, with the full account in its own comment, not deleted, so it can be re-enabled once actually proven safe). Generate now publishes ONLY corePayloadFor\'s minimal shape -- identity, current session, squad sessions -- which has now been independently proven fast and reliable twice (this exact build\'s own predecessor reaching load_qr in 2.5 seconds for William Callow, and the training-view-window fix before that) -- and nothing else runs afterward that could freeze the phone. Trade-off, stated plainly rather than hidden: a swimmer\'s performance/training/test/meet portal sections will stay empty (swimmer-portal.js already renders that gracefully -- "0 events", "0m · 0 sessions", "No ... loaded yet.") until the analytical pipeline is properly bounded and this is re-enabled -- including for any swimmer who had richer data published before tonight, since every Generate republishes and overwrites that athlete\'s payload row with the minimal one. Regression: rewrote tests/qr-generate-core-then-background-enrichment-20260920.cjs (previously proved enrichment fires and succeeds; now proves the opposite -- exactly one publish call ever happens, no second call and no enrichment* breadcrumb field appear however long you wait -- plus a source-level guard reproducing the exact risk if the commented-out block is ever uncommented again without first proving payloadFor() is bounded). Reverted tests/qr-generate-concurrent-attempt-guard-20260919.cjs back to its simpler pre-enrichment shape (the enrichment-aware waiting it briefly needed no longer applies since nothing runs after \'done\' at all). Full sweep: same standing 21-failure baseline, zero new regressions, 132 passing. **Standing lesson, now proven twice in one evening**: reading this pipeline\'s code and concluding a given step is bounded has failed to catch a real bug three separate times tonight (national-benchmark, training-history, and now the background-enrichment scheduling flaw itself) -- do not re-enable or extend this pipeline\'s analytical work again without first counting real operations against data at Andy\'s actual volume, the way both of the two CONFIRMED fixes tonight were actually verified.\n\nPrior note (core-payload-background-enrichment build, now known to have its own flaw -- kept verbatim for the history it documents): Andy, live, right after the training-view-window build below shipped and unblocked "Give swimmer access" far enough to reach a THIRD analytical step this same evening: "Come on Claude this is enough, can you sort it out or do we need to find an easier way to give swimmers access" -- alongside a fresh breadcrumb for a different swimmer, William Callow, tickSeconds stuck at 0 with wakeLockHeld:true again, lastCheckpoint this time pinned to \'payload:performance\'. That is the THIRD genuinely different step (national-benchmark check, then athleteTrainingView, now safePerformance/buildAthletePathways) implicated in ONE evening inside the same payloadFor() pipeline -- a pattern, not a coincidence: this pipeline had apparently never been run end-to-end against Andy\'s real production data volume before tonight. An exhaustive static read of safePerformance()\'s entire call chain (performanceEngine.pathwaysForAthlete -> buildAthletePathways in performance-pathway-ck.js -> buildEventLadder/rowsByProgramme/chooseProgrammeRows, all tracing back to the same M.refs pathway_standards/pathway_meets tables already confirmed empty; performanceEngine.rankedEvents -> race-pace.js -> wa-points.js, also cached; app.js\'s own separate older pathway system, confirmed to read the SAME M.refs registry) found no further plausible unbounded loop -- every one either already terminates on empty reference data or is properly bounded. Rather than keep chasing individual slow steps reactively under pressure, each fix only to reach a new one, this is the structural answer to Andy\'s actual question: it stops computing ANY of the rich analytical sections (performance ladders, training accumulation, test history, meet history, shared evidence) in Generate\'s own critical path at all. engines/swimmer-invite-bn.js\'s corePayloadFor() builds ONLY identity + current session + squad sessions -- exactly what Andy asked for on 19 Sept ("I just want to give them access to their info and their squads sessions") and already fast/bounded (sessionsPartFor/candidateSessionsFor, at most 8 sessions) -- and Generate now awaits and publishes THAT immediately. The full, analytical payload (built by the unchanged payloadFor()) is computed and republished afterward via a second msos_publish_swimmer_payload call, deferred one tick (setTimeout(...,0)) so it can never delay the QR/status a coach is waiting on, and entirely outside GENERATE_TIMEOUT_MS so it can never trigger the whole-flow timeout either. msos_publish_swimmer_payload is a plain upsert keyed by athlete_id (confirmed against supabase/20260824_secure_swimmer_portal.sql: `on conflict(athlete_id) do update ...`), so this second publish safely updates the SAME record in place -- no new invite or device pairing needed, and the swimmer\'s next portal open (or a manual refresh) picks up the fuller payload automatically. swimmer-portal.js already renders every analytical section gracefully when empty (performance: "0 events"/"Tap for pathway"; training: "0m · 0 sessions"; tests/meet: "No ... loaded yet.") -- confirmed by reading its own rendering code, not assumed -- so a swimmer opening the link in the first few seconds sees a working portal with their session, just without the extra analysis until it lands moments later. The background republish\'s own outcome (ok/error/threw) is recorded on the breadcrumb as enrichmentOutcome/enrichmentMessage/enrichmentAt, and each of its sub-steps as enrichmentStep, purely via the same silent writeAttempt() mechanism -- it never touches setStatus/mark/note, so it can never overwrite the "Ready" status the coach already sees. This eliminates the entire risk category (any future slow analytical step, known or not-yet-found) from Generate\'s critical path, rather than requiring another reactive fix the next time a different step turns out to be slow on Andy\'s real data volume. Regression: new tests/qr-generate-core-then-background-enrichment-20260920.cjs (proves the critical-path publish carries corePayloadFor\'s minimal shape even with real non-empty analytical data available in the fixture; proves a second publish call follows automatically carrying the full enriched payload; proves a failed background republish is recorded as enrichmentOutcome:"error", not silently swallowed, without failing the already-succeeded attempt; fail-before/pass-after confirms reverting the one-line corePayloadFor swap makes the critical-path publish carry the full analytical payload again, reproducing the exact risk this fix removes). Updated tests/qr-generate-concurrent-attempt-guard-20260919.cjs (the winning attempt\'s own legitimate background-enrichment writes that now follow "done" are real, wanted new behaviour, not the stray-superseded-ticker bug that test exists to catch -- its assertions now wait for that enrichment to settle and confirm the core done/outcome/resolvedAt fields survive it, before proving no further write of any kind ever lands) and tests/swimmer-generate-qr-model-progress-20260913.cjs (its literal-source-match for the payload-assembly status-line wiring updated from payloadFor to corePayloadFor -- the wiring pattern itself is unchanged). Full sweep: same standing 21-failure baseline, zero new regressions, 132 passing (131 plus this build\'s own new test). Still open, and now moot rather than resolved: whether safePerformance genuinely had a fourth unbounded-computation bug for William Callow\'s specific data, or whether that breadcrumb was simply captured early during normal execution -- this fix routes around the entire computation regardless, so it no longer matters which.\n\nPrior note (training-view-window build, superseded by the above -- kept verbatim for the history it documents): Immediately after the national-benchmark-not-blocking build below shipped, Andy hit a REAL freeze again -- "Hahahah now it freezes as soon as I click generate QR" -- but this time a genuinely different one: the breadcrumb showed tickSeconds stuck at 0 for 28+ minutes with wakeLockHeld:true (tab never backgrounded), which is a true synchronous main-thread block, not an await/network hang a timeout could ever catch. lastCheckpoint pinned it to \'payload:training\' -- a step the flow had never actually reached before tonight, since every earlier attempt either hit the removed evidence-refresh freeze or the just-fixed instant national-benchmark block first. Root cause, found in architecture/training-history-core.js\'s athleteTrainingView(): it built its `records` array by mapping EVERY session ever recorded (176 on Andy\'s real device, and growing every week) through recordSession(), and recordSession() runs TWO full projection() passes per session, each calling the real per-item prescribe() (engines/coordinator.js\'s prescription()) once per set/rep item -- genuine pace/target computation, not a cheap lookup. Confirmed nothing downstream ever needed more than a trailing window: `today` only needs the current date\'s session, `week`/`month` only ever look back 7/30 days, and `upcoming` was already computed separately and cheaply (only the 4 nearest future sessions, never all of them); no caller (coach-loop-ui.js, swimmer-instant-open-cn.js, the swimmer-portal payload) reads the full `.records` array for anything else. So this function silently re-did real per-item work for months of irrelevant sessions on every single call, and on Andy\'s real data volume that was apparently enough to lock up the main thread for many minutes straight -- explaining both this freeze AND, in hindsight, plausibly contributing to some of the earlier same-night freeze theories that were never fully pinned down (buildModel/invalidate stretch) before the instant-access redesign made that particular stretch moot. Fixed: `records` is now built only from sessions within a 60-day trailing window (comfortable margin over the 30-day month summary; a session with an unparseable date is kept rather than silently dropped; future sessions are never excluded since their timestamp is always >= now). Regression: new tests/training-history-view-unbounded-recompute-20260920.cjs (counts real prescribe() calls rather than timing, which would be flaky -- proves old sessions are skipped while today/week/month/upcoming stay correct; fail-before/pass-after reproduces 320+ calls on the old unconditional source vs. a handful bounded). Full sweep: same standing 21-failure baseline, zero new regressions, 131 passing (130 plus this build\'s own new test). Still open: whether this was the ONLY thing capable of producing a true main-thread freeze in this flow, or just the first one actually reached and proven -- if a freeze recurs with a NEW lastCheckpoint value never seen before, that is real signal, not cause to assume this fix failed.\n\nPrior note (national-benchmark-not-blocking build, superseded by the above -- kept verbatim for the history it documents): Immediately after the qr-instant-access build below shipped, Andy hit a NEW hard block, instantly, every time: the freeze was gone but "Give swimmer access" for Matthew Robertson now failed at once with "Swimmer access held: No upcoming verified SCM national benchmark is linked." Andy: "This is no help to me." / "If there\'s an issue with upcoming meets we need to sort that too." Root cause: engines/swimmer-performance-ci.js\'s readinessFor() carried a third check requiring at least one event to have a linked upcoming national standard -- data that only ever comes from the pathway_standards/pathway_meets reference tables, which are only ever populated by completeEvidence(). The build below made completeEvidence() fire-and-forget in the background specifically so a slow/failing fetch could never freeze Generate again -- but that also means nothing can populate those tables before this synchronous check runs anymore, so it now fails unconditionally on any cold cache, which is Andy\'s actual chronic real-world state (pathway_standards has sat at 0 rows all session, every breadcrumb, all night). Fixed by dropping that third check entirely from readinessFor() -- confirmed safe because swimmer-portal.js already renders an event with no linked next target gracefully ("Tap for pathway" in the event summary line, "No pathway marks loaded." in the expanded detail); nothing downstream assumes every event has a linked national benchmark. Checks 1-2 (date of birth present, at least one verified race event loaded) remain hard blocks -- they are the swimmer\'s OWN evidence, never starved by an unawaited background step, so this fix does not touch them. Regression: new tests/qr-generate-national-benchmark-not-blocking-20260920.cjs (Matthew\'s real shape with an empty reference cache now passes readiness, the two remaining checks still correctly block, a source-level proof that swimmer-portal.js already degrades gracefully without a linked next target, fail-before/pass-after on the exact readinessFor() source change); updated tests/pathway-standards-reference-staleness-20260918.cjs, whose sanity/fail-before assertions previously read readinessFor().ok as a proxy for "the national-benchmark cache recovered" -- since that gate no longer exists, both now assert directly on buildModel()\'s events carrying the correct (or stale/absent) linked target instead, which is what those sub-tests actually cared about. Full sweep: same standing 21-failure baseline (20 Playwright/local-server-dependent + the flagged styles.css hash mismatch), zero new regressions, 130 passing. This does not touch why pathway_standards/pathway_meets have never once synced successfully on Andy\'s device all session -- that remains open and worth a look separately, but is no longer able to block access in the meantime.\n\nPrior note (qr-instant-access build, superseded by the above -- kept verbatim for the history it documents): A SIXTH occurrence overnight (Matthew Robertson, genuinely frozen this time -- resolvedAt/outcome both null, lastCheckpoint stuck on cache_invalidate, no connection issue per Andy) proved the refs-save-outcome diagnostics build from a few hours earlier had not found the real cause either: refs_save/t400_hydrate/cache_invalidate had all already fired, so the hang had moved past the whole evidence-refresh/local-cache-save stretch and into the handful of synchronous calls right after it (buildModel/performanceEngine.invalidate/pathwayPbCache.clear/MSOSEvidenceIndex.invalidate) -- plausibly a full-state recompute over data that has grown substantially since this flow was built (672 coach results, 152 test results, 134 captures, 72 athletes, 211MB of local storage this same night). At that point Andy said plainly: "Maybe the QR isn\'t the best way... I just want to give them access to their info and their squads sessions, this back and forth is wearing me down for 1 simple task." He\'s right, and rather than keep chasing a sixth theory for the same mechanism, this build removes that mechanism from the button\'s critical path entirely. Every freeze chased across 19 Sept -- wake lock, visibility, the concurrency guard, storage/checkpoint diagnostics, refs-save-outcome diagnostics -- traced back to one place: engines/swimmer-invite-bn.js\'s Generate handler awaiting M.swimmerPerformanceBM.prepareAthlete(), which runs a live completeEvidence() network refresh (PB board, coach results, event history, test results+types, and -- worst of all -- a full ~4400-row pathway_standards/pathway_meets refetch whenever the local copy is empty, which it always was on Andy\'s phone) before it will hand over access at all. That live call is now GONE from Generate\'s critical path. Access is built synchronously from whatever is already cached locally via readinessFor()+payloadFor() -- both were always synchronous and read-only, never the source of any freeze -- which already includes the swimmer\'s squad sessions (sessionsFor()/candidateSessionsFor() pulls every session in the swimmer\'s squad(s) across a real date window, not just "today"; this was never limited to one individual session, so no scope change was needed there). A completeEvidence() call is still kicked off immediately, but strictly fire-and-forget: never awaited, never able to block or fail Generate, its eventual outcome recorded on the breadcrumb as backgroundRefreshOutcome/backgroundRefreshAt purely so the cache keeps improving for next time and so a future investigation still has something to look at if needed. This does not explain or fix whatever the buildModel/invalidate-stretch main-thread stall actually was -- it makes that entire stretch irrelevant to this button by never putting it in the critical path to begin with. Regression: updated tests/swimmer-secure-onboarding-bn.cjs (prepareAthlete gate genuinely gone, background refresh call present), tests/swimmer-generate-qr-model-progress-20260913.cjs (live onJob wiring into Generate genuinely absent; prepareAthlete\'s own behavior, tested directly, is unchanged), tests/qr-generate-concurrent-attempt-guard-20260919.cjs / tests/qr-generate-wake-lock-20260919.cjs / tests/qr-generate-attempt-breadcrumb-20260916.cjs (their "stuck attempt" fixtures moved from a hanging prepareAthlete to a hanging msos_bootstrap_owner RPC, now the first genuinely awaited step), tests/qr-generate-refs-save-outcome-diagnostics-20260919.cjs (its invite-level assertions rewritten for the new fire-and-forget backgroundRefreshOutcome field, replacing the removed diagRefsSaveOutcome wiring), tests/swimmer-invite-double-generate-20260907.cjs (double-submit guard\'s search window widened for this build\'s added comments). Full sweep: same standing 21-failure baseline (20 Playwright/local-server-dependent + the flagged styles.css hash mismatch), zero new regressions, 129 passing.\n\nPrior note (refs-save-outcome-diagnostics build, superseded by the above -- kept verbatim for the history it documents): FIFTH+ occurrence, same day, on the storage-diagnostics build just shipped: the newest breadcrumb finally looked DIFFERENT from every prior one -- it did not hang forever, it settled after roughly 3 minutes with an honest error, caused by two real 12-second network timeouts (training_test_types -- only ~10 rows -- and pathway_standards). Both jobs timing out, including the tiny 10-row one, points at Andy\'s connection genuinely struggling in that window rather than a client-side infinite loop; this has been said to him plainly, along with a direct question about whether his connection was patchy at that moment, and whether the OLD symptom (ticker frozen at literal 0s forever, no error, ever) has still been happening lately or whether this resolved-with-an-error shape is what he has actually been seeing recently -- those may not be the same bug. The same breadcrumb\'s diagStateCounts also showed pathwayStandards and pathwayMeets both sitting at 0 rows in local memory, meaning neither had a usable cache going into that attempt -- forcing a full ~4400-row pathway_standards refetch on every single QR attempt regardless of whether that swimmer\'s check even needs it, and on a slow connection that refetch is exactly what is tipping over the 12-second job timeout. What was still not known after the storage-diagnostics build: completeEvidence()\'s post-loop M.refs.save() call (the IndexedDB write meant to persist a successful fetch for next time) is wrapped in a withTimeout()+try/catch that discards EVERY outcome identically -- clean success, silent false-return failure, timeout, and thrown error all look exactly the same from outside, nothing on screen or in the breadcrumb ever changed. That means there was no way to tell whether a pathway_standards fetch that DOES succeed on some attempt is actually being cached, or whether the save itself is quietly failing every time -- which would by itself explain the persistent 0-row local cache independent of the network timeouts. This build records that outcome (one of ok/failed/timeout/error) and its real duration on completeEvidence()\'s own return value, threaded through prepareAthlete()\'s .completion and onto the breadcrumb as diagRefsSaveOutcome/diagRefsSaveMs -- written even when the overall attempt ultimately errors, since Andy\'s real case did. This changes NOTHING about what happens on any outcome: a failed, timed-out, or throwing save still never blocks or fails the flow, exactly as before. Regression: tests/qr-generate-refs-save-outcome-diagnostics-20260919.cjs (all four outcome shapes -- ok/failed/timeout/error -- correctly classified with a real elapsed-time figure, propagation through prepareAthlete\'s completion, the breadcrumb write reproducing Andy\'s own error-outcome shape, both fail-before/pass-after against the real source for both files touched). This is explicitly NOT a fix for either the network timeouts or the true frozen-forever occurrences from earlier today -- it exists so the next breadcrumb (or Andy\'s answer about his connection that night) finally says whether the empty pathway_standards/pathway_meets cache is a fetch problem, a save problem, or both. Full regression sweep: same standing 20 Playwright/local-server-dependent baseline failures plus the one already-flagged styles.css hash mismatch, zero new regressions, plus this build\'s own new test passing.\n\nPrior note (storage-diagnostics build, superseded by the above -- kept verbatim for the history it documents): FOURTH+ occurrence, same day, on the concurrency-guard build just shipped: the concurrency fix below did NOT hold, and two facts Andy confirmed directly rule it out as the cause rather than just failing to hold -- (1) the freeze now reproduces for EVERY swimmer he has tried, not only Matthew Robertson, and (2) it survives a full app reload, which leaves no leftover in-memory attempt for a new one to collide with. So this is not the two-attempts-corrupt-each-other bug; it is something else, still unfound, that happens to land on the exact same step every time. Separately, and only because Andy went looking on his own initiative, he checked Chrome\'s own per-site storage figure for this app and found 223MB stored -- for an offline-first app whose real per-athlete evidence should be single-digit-to-low-double-digit MB, that is a strong sign something local has grown unbounded, but there is deliberately NO storage clear attempted here: Andy has marked very few sessions "Finished" while chasing this bug, so a chunk of that 223MB may be genuine, not-yet-synced coaching work (sessions, notes, possibly captured photo/video), and this app\'s own standing rule is that user data is never cleared as a repair strategy. Instead, two purely-observational, read-only diagnostics were added, neither changing behavior nor blocking the flow if unsupported: engines/swimmer-performance-ci.js\'s completeEvidence() now fires two extra non-indexed checkpoints per evidence job (immediately after that job\'s network fetch resolves, and immediately after its merge into M.state completes), each naming the real fetched-row count and the real pre/post-merge array length for that job\'s own state slot -- so a future freeze\'s breadcrumb will show whether the hang is inside the fetch/timeout wrapper despite Supabase proving success, or specifically inside the merge, and whether that array was already large before the run even started. engines/swimmer-invite-bn.js\'s Generate handler now also takes one navigator.storage.estimate() snapshot plus the in-memory length of every evidence/reference array the flow touches, written to the breadcrumb before any network activity begins, so even a freeze that never reaches a single evidence job leaves behind real MB/row-count numbers instead of a Chrome settings screen alone. Regression: tests/qr-generate-storage-diagnostics-20260919.cjs (fetched/merged checkpoint values and ordering, storage-estimate + state-count snapshot recorded and reported honestly as null when unsupported, both fail-before/pass-after against the real source). This is explicitly NOT a fix -- the real cause of the freeze is still unknown -- it exists so the next occurrence\'s breadcrumb finally narrows where the 223MB (or whatever it turns out to be) actually sits, instead of continuing to guess. Full regression sweep: same standing 20 Playwright/local-server-dependent baseline failures plus the one already-flagged styles.css hash mismatch, zero new regressions, plus this build\'s own new test passing.\n\nPrior note (concurrency-guard build, now known incomplete -- kept verbatim for the history it documents): THIRD same-day freeze for Matthew Robertson, same exact symptom, on the diagnostics build shipped earlier today -- and this time the new breadcrumb fields actively DISPROVED both standing theories: wakeLockHeld:true (the lock never let go) and no lastVisibilityState/lastVisibilityChangeAt at all (the tab never went hidden by any measure the Page Visibility API can see). So this is NOT a locked/backgrounded-tab suspension, and treating it as one for two straight builds was a real miss. Went back to live Supabase edge/postgrest logs for this exact occurrence rather than guessing a third mechanism: all 5 evidence-job network calls for Matthew genuinely completed in under 2 seconds, and unrelated background app activity (the ~25-36s coach-dashboard reconcile poll) kept firing normally throughout the reported freeze window -- ruling out both a network hang and a full engine-level JS deadlock. Re-reading the exact code path between the evidence loop finishing and the next RPC firing (engines/swimmer-performance-ci.js\'s completeEvidence/prepareAthlete/readinessFor, engines/swimmer-invite-bn.js\'s own onclick handler) found no plausible UNBOUNDED hang there -- every step is either synchronous or already timeout-protected. That contradiction (code that cannot get stuck there, breadcrumb showing it stuck there every time) pointed at how ATTEMPTS themselves were tracked rather than the evidence/RPC chain: the only guard against two "Generate" flows running at once was genBtn.disabled, scoped to ONE modal\'s ONE button. Closing the modal (Close button or the phone\'s back button) never cancelled an in-flight attempt -- its ticker, wake lock, visibility listener and any pending network/RPC calls kept running in the background for up to the full 2-minute ceiling, invisibly. Andy retrying "Give swimmer access" after a frozen-looking attempt -- exactly what three same-day occurrences on one athlete imply -- opened a brand new modal with a freshly-enabled button, with zero awareness of that still-running old attempt: both wrote to the SAME shared msos_qr_last_attempt breadcrumb key on every tick, so whichever wrote last silently overwrote the other\'s real progress. A genuinely slow-but-working retry could have its status stomped by an old, abandoned attempt re-stamping its own frozen step every second -- indistinguishable from an eternal freeze, and burning a full duplicate evidence-fetch + RPC round trip on every retry. Fixed: a module-level activeGeneration now tracks whichever attempt may touch the screen or the breadcrumb; starting a new attempt, or closing its own modal, cancels whatever it was previously pointing at, so at most one attempt is ever live. Also added lastCheckpoint/lastCheckpointAt, written directly and independently of the on-screen ticker, at every step from the evidence-job loop through the reference-cache save, the pathway model build, and each of payloadFor\'s six sub-steps -- steps that had never once appeared in three occurrences\' worth of breadcrumbs -- so if this specific gap resurfaces even once more, the next breadcrumb will finally show whether the code genuinely never reaches that point or reaches it fine while only the on-screen status is lost. Being upfront: this is a real, verified concurrency bug with a regression test proving it reproduces and is fixed (tests/qr-generate-concurrent-attempt-guard-20260919.cjs) -- but after two prior "fixes" that didn\'t hold, it is not being called a confirmed final answer to every freeze Andy has seen; the added checkpoints exist specifically so a further occurrence, if there is one, finally pins down anything this does not already explain. Also rolled into this same build, at Andy\'s request, two genuine pre-existing test failures that were unrelated to the freeze investigation: (1) v4-correct.js\'s own Guardian self-check "T400 roster starts from Here/Modified and cross-squad add appends" had Sophie Newlove seeded active:true in its fixture, when CLAUDE.md 4.33/6.9 both document her real recovered-roster status as correctly inactive -- the roster logic itself (presentRosterIds) was never wrong, the fixture had just drifted from that documented fact, so it is fixed there rather than by changing any real roster code; (2) tests/swimmer-secure-onboarding-bn.cjs had three assertions pinned to exact historical `?v=` cache-bust tags for swimmer-invite-bn.js/swimmer-performance-ci.js/swimmer-instant-open-cn.js that had simply been superseded by ordinary version bumps since (most recently by this same build) -- loosened to check each engine is loaded at all, which is what each assertion\'s own failure message actually says it is checking; a fourth assertion was still pinned to a stale pre-refactor literal (`tests:safeTests(a)`) from before this same 19 Sept checkpoint work moved that call one line earlier -- the underlying payload still genuinely carries both fields, confirmed by an equivalent shorthand-aware check. Neither was a real product regression. Full regression sweep: same 20 pre-existing Playwright/local-server-dependent baseline failures (need a dev server on 127.0.0.1:8765 not run in CI here) plus the one already-flagged styles.css release-package hash mismatch, zero new regressions -- down from 23 failures at the start of today\'s work.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
