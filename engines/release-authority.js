'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-hub-double-tap-fix-20260916a';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260916-hub-double-tap-fix';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-16T00:00:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Andy: "I hit coach hub and nothing happens, I click it again it freezes. Stays there till I back out." engines/coach-loop-ui.js\'s renderCoachHub() walks nearly all of M.state synchronously on every visit -- every weekly/season plan, every canonicalSession scanned for carry-forward, every block/item for the session-makeup mix, plus captures/reflections/meetRaces lookups -- with no caching, so as months of real history accumulate (the same "state has grown large, cloud sync off" pattern behind the QR-freeze/background-save investigation) this can take long enough that nothing visibly happens on the first tap. engines/navigation.js\'s real click handler (V.go) has no guard against re-navigating to a view that\'s already active, so an impatient second tap on Coach Hub queued a full SECOND run of the same expensive computation right behind the first -- not concurrent (JS is single-threaded), but back-to-back, roughly doubling the wait and crossing into what reads as a genuine freeze. Fixed by coalescing a rapid repeat render: if nothing in state changed (storageRevision unchanged) and the last real render finished under 400ms ago, a repeat call is now a no-op instead of redoing the whole computation. Also added real render-duration timing (M.viewTimings.hub), surfaced on the Connection page ("Last Coach Hub render") and Copy Diagnostics, same diagnostics-first pattern as the background-save timing -- so if Hub still feels slow on a single (non-double-tap) visit, the next report carries a real number for the underlying render cost itself rather than another guess. Regression test: tests/coach-hub-double-tap-freeze-20260916.cjs (proves a genuine state change still re-renders, a same-state repeat is coalesced, and fail-before/pass-after on the exact fix). Send-off window-match fix (v4-send-off-window-match-20260916a) and background-save timing diagnostics (v4-background-save-timing-diagnostics-20260916a) both remain in this build unchanged. No existing session-selection, target, race-pace, T400, meet, access-authority, reference-sync, roster-integrity, per-rep-effort-labeling, mod-reason-display, or IM-parsing behaviour is changed by this release.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
