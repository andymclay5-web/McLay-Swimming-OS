'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-dosage-structural-default-20260918a';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260918-structural-default';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-18T00:00:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Fourth fix in the "Session Methodology dosage classification" area (see msos-known-issues.md), on top of the same-day cues fix (v4-dosage-cue-classification-20260918a). That build fixed the "MAX" cue gap but left a bigger honest gap unfixed: roughly 3200m of Andy\'s real 4250m session -- the warm-up stroke ladder, the pre-set breakdown, and critically the entire 1600m post-set 4x400 IM/Freestyle/IM/Choice -- still reported Unclassified, because none of those lines carry ANY zone/intensity keyword anywhere (not raw, not cues, not zone). Andy was asked directly whether the classifier should apply a structural default for entirely untagged sets rather than staying Unclassified; he answered with his own coaching logic: "all easy swimming would fit into Aerobic Capicity and/or aerobic development or regentration... a hard 400 prob is threashold, a hard 200 is prob cl, a hard 100 is prob 400 to 200p... max is max, atp cp for short dist... through the other anaerobic zones based on dist and rest" -- and explicitly asked for this to be inferred from STRUCTURE (distance + rest), not from a typed keyword like "hard". Implemented as a last-resort default inside systemFrom(), firing only when no keyword anywhere matched: a real distance-bearing item with a genuine authored rest (item.restSeconds, e.g. from "Rest · 20 sec" -- distinct from a send-off/cycleSeconds interval) is treated as "worked" and classified by Andy\'s own named distance ladder (400+ metres -> Threshold, 200-399 -> Clearance, 100-199 -> Race pace, under 100 -> Speed / Max / ATP-CP); an item with NO authored rest at all is treated as continuous/easy swimming -> Development. An item with no real distance at all (a bare fixture, a genuinely non-distance line) is untouched -- still Unclassified, exactly as before, since there is nothing to structurally default from. Deliberately NOT added: an "Overload" branch within this ladder -- Andy named only the four anchors above, and Overload stays reachable only via its own explicit keyword until he gives a concrete case; a finer rest-ratio gradient beyond the simple "has real rest or doesn\'t" signal, for the same reason. Verified end-to-end against Andy\'s real session text: Unclassified drops all the way from 3650m (pre-17-Sept) / 3200m (post-cues-fix) to 0m; the 1600m post-set correctly lands as Threshold; the warm-up/pre-set/main-set lines with no authored rest correctly land as Development (1600m); the cues fix\'s own 450m of Speed / Max is unaffected; the Coach Hub/Board "SESSION METHODOLOGY" top-2 banner now reads "Threshold 45% · Development 24%" instead of "Unclassified 55% · Speed / Max 24%". Regression test: tests/dosage-structural-default-20260918.cjs (unit-level ladder/no-rest/no-distance cases plus a full end-to-end session-dose check against Andy\'s real text; fail-before/pass-after). tests/dosage-zone-raw-text-fallback-20260917.cjs and tests/dosage-cue-classification-20260918.cjs were both reworked in lockstep to revert only the single source line each one is actually testing (rather than re-snapshotting the whole function, which had already broken twice today as later edits landed below each prior one) -- both keep proving only their own narrow claim, unaffected by this new default; the cues-fix test\'s own end-to-end assertion was narrowed to just its own 450m Speed / Max claim, since the overall Unclassified-%/banner is now this fix\'s number to own, not its. Full regression sweep confirms the same 22 pre-existing baseline test failures recur identically before and after (all meet-*, product-acceptance, session-intake, guardian, release-package, and the known styles.css hash-mismatch failure -- none touched by this fix; meet remains shelved). Hash-prefixed rep-pattern fix, ladder full-count fix, equipment-vocabulary fix, Coach Hub double-tap fix, send-off window-match fix, background-save timing diagnostics, the QR-generate-attempt breadcrumb, and the update-toast dedupe/tap-reload fix (16-17 Sept) remain in this build unchanged. No existing session-selection, target, race-pace, T400, meet, access-authority, reference-sync, roster-integrity, per-rep-effort-labeling, mod-reason-display, equipment-vocabulary, ladder-composition, or rep-pattern-parsing behaviour is changed by this release.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
