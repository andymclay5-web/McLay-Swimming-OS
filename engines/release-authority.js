'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-mod-reason-declutter-qr-diagnostics-20260913c';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260913-mod-reason-declutter-qr-diagnostics';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-13T00:00:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Two fixes. (1) Board declutter: Andy asked that "all the explanation of why on the mod side don\'t need to show n the board" -- engines/board.js\'s modCell no longer renders the adaptationReason/evidence-provenance explanation line under a modified swimmer\'s row on the live Board. The underlying data is unchanged: modification.js still computes adaptationReason for every adapted item, and session-methodology.js\'s own flagged-for-review summary reads it independently of anything board.js renders -- only the Board display of it is gone. (2) QR-generate diagnostics: Andy repeatedly reported the "Give swimmer access" QR modal "stopping at 5/5 0s". buildModel() and the payload-assembly step that run after the evidence-jobs loop finishes had no status update of their own, so any time either took was silently misattributed to whichever evidence job ran last, making a stuck-there case indistinguishable from a genuinely stuck evidence check. Both steps now get their own status line ("Building pathway model…" / "Assembling private swimmer view…") so a future freeze report will name exactly which phase is slow or stuck. No existing session-selection, target, storage, race-pace, T400, meet, access-authority, reference-sync, roster-integrity, per-rep-effort-labeling, or IM-parsing behaviour is changed by this release.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
