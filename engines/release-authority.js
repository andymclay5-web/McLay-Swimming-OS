'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-board-real-session-vocabulary-20260918b';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260918-board-real-session-vocabulary';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-18T12:00:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Ninth fix today. Andy pasted his real 18 Sept session text (WARM-UP/RACE SIMULATION/FINS/RACE QUALITY/WARM-DOWN) plus live Board screenshots, then separately flagged three of his own observations: modified swimmers wrongly shortened from "75% of #1 Event"; Charlotte\'s 50s landing on a 2:00 cycle (45+ sec rest) when the main group gets 15-30 sec rest on the same set; and a "Dive Start 50 #1 form" / "@100 pace" item never triggering a race target. Investigated all seven symptoms by tracing the real code paths (not guessing) before writing anything. Block-header vocabulary gap: heading() (app.js) only recognised 6 hardcoded block keywords, so "RACE SIMULATION", "FINS" and "RACE QUALITY" -- none of them -- silently fell back to CONTENT inside whichever block was already open (WARM-UP), collapsing 5 authored sections into 3 Board tabs, violating CLAUDE.md 2.26 ("unknown coaching language must remain intact, not deleted"). Fixed: any standalone ALL-CAPS line not matching strongLocalBoundary\'s own within-block phrases (aerobic reset, back/front-end work, quality/race finish, warm-down/cool-down, sharpeners, reset block) and not the internal "MSOS " sentinel prefix now opens its own titled block. Percent/duration misparse: singleDistance() (app.js) read any line starting with 2-4 digits as a metres distance, so "75% of #1 Event" became a 75m swim (individually shortened like any other distance -- this alone explains Andy\'s first observation) and "45 min Individual Race Warm-Up" became a bare, unit-less 45m phantom distance. Fixed: numbers immediately followed by "%" or "min(ute)(s)" are excluded, and an optional leading "dive"/"push"(" start") prefix is now recognised so "Dive Start 50 #1 form" (descriptive words before the number) still parses as a real 50m item. "Choice" stroke stripping: cueText()/workLabel()/strokeInfo() (engines/board.js) all special-cased the literal string \'Choice\' out of the rendered label/cue text -- but Evidence.stroke(\'\')  only ever returns \'\' for "no stroke", never \'Choice\'; that string can only appear when Andy typed the word himself, so there was no real "meaningless default" being protected, just a deliberate instruction (stroke choice under race conditions) being deleted every time. Removed all three exclusions. "Descend 1-4" orphaning: parseBlock()\'s group-close logic (app.js) reset current to null the instant an "N Rounds:" group closed, so a trailing descriptor line right at the close boundary (e.g. "Descend 1-4" under an indented "4 x 50 @ 1:00") became a disconnected top-level cue instead of attaching to the set it describes. Fixed by remembering the last set in the closing group so the closing line itself can still reach it. Charlotte\'s disproportionate rest: isQuality() (engines/modification.js) was missing "descend"/"descending" from its keyword list, so a short descend/quality 50 set fell through to the generic no-comparator reps-reduction fallback, which (correctly, for genuine aerobic/endurance sets) stretches the interval to preserve the swimmer\'s TOTAL time-in-water window -- but applied to a quality set that math gave Charlotte a 2:00 cycle against the squad\'s 1:00, far outside the work:rest ratio the set is meant to deliver (CLAUDE.md 2.41-2.45). Added "desc(?:end|ending)?" to the keyword regex, routing descend sets through the quality-specific evidence-based paths instead. Confirmed via git-stash A/B comparison this changes nothing for any existing kick or IM test fixture (both reach separate, quality-independent branches). Missing Dive Start race target: engines/race-pace.js already implements the exact dive/push/named-segment target model Andy described (dive triggers the start segment target, "@ pace" alone signals the non-dive average, an explicit ordinal/Nth-50 or Nth-25 triggers that segment\'s own actual target) -- the bug was never in that engine. "Dive Start 50 #1 form" and its "@100 pace" cue arrive as two separate lines, and raceIntent was only ever read from an item\'s own initial raw text, never backfilled from a later cue line, so the item stayed a plain non-actionable cue and never reached the race-pace engine at all. Fixed by backfilling raceIntent from the cue line in parseBlock()\'s cue-attachment branch. Regression test: tests/board-race-simulation-fins-quality-20260918.cjs -- drives Andy\'s real verbatim pasted session text through the full live pipeline end-to-end, asserting all 5 blocks appear with his own titles, the 75%/45-min items are never read as distances and are never shortened, both Choice items keep the word "Choice" in their rendered text, the descend 50 set classifies as quality, and the Dive Start 50 resolves an \'ok\' race-pace target via the start-segment branch -- plus git-HEAD-sourced fail-before proofs for each of the 4 most significant fixes. Full regression sweep (145 files) confirms the same 23 pre-existing baseline failures recur identically before and after (release-package.test.js\'s known styles.css hash mismatch untouched, meet-*.js untouched), zero new regressions. Everything from the eighth fix (QR-generate pathway_standards pagesize) and earlier today remains in this build unchanged.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
