'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-qr-wake-lock-diagnostics-20260919b';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260919-qr-wake-lock-diagnostics';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-19T19:05:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'THE WAKE-LOCK FIX SHIPPED EARLIER TODAY (v4-qr-wake-lock-fix-20260919a) DID NOT HOLD -- this is diagnostics, not a second confirmed fix. It froze again for Matthew Robertson, same day, same build, same exact symptom: "Checking swimmer evidence... (5/5 * training_test_types) (0s)", "whole screen froze again, could only get out by hard back". The breadcrumb showed it stuck barely 1.7s into that step with resolvedAt still null -- consistent with total JS suspension again, not a slow request (the per-job 12s timeout never fired either, and that timer would also be paused by the same suspension). So the earlier diagnosis (a locked/backgrounded page gets its JS suspended by Android) was very likely still directionally correct -- but the fix built on top of it (a screen wake lock) evidently did not prevent whatever actually happened this time. On reflection, without new guessing: the Wake Lock API only ever prevents the screen dimming from ordinary INACTIVITY timeout. It does NOT prevent a manual power-button press, and does NOT prevent Android throttling a BACKGROUNDED tab if the coach switches to another app (a text message, phone in pocket) even with the screen still on and even with a wake lock still held -- "screen was locked" could have meant any of these, and only the plain-inactivity case was ever fixable this way. Rather than guess a third specific mechanism, this ships pure observation instead: engines/swimmer-invite-bn.js\'s Generate handler now writes three new fields to the existing msos_qr_last_attempt breadcrumb -- wakeLockSupported/wakeLockHeld (was the lock even available and actually acquired this time, so a future silent-acquisition-failure is directly visible instead of assumed), wakeLockReleasedEarly/wakeLockReleasedAt (did the BROWSER itself force-release the lock before we did -- its own native \'release\' event, which only fires when the OS reclaims it, e.g. a real hardware screen-off overriding the app), and lastVisibilityState/lastVisibilityChangeAt (did the tab itself go hidden -- truly backgrounded, not just dimmed -- and exactly when, so it can be compared against the moment the ticker stopped). The wake lock itself is left in place (it can only help, never hurt, and did not cause this recurrence), but is no longer being relied on as a full fix. Regression: tests/qr-generate-wake-lock-20260919.cjs, rewritten to prove all three new breadcrumb fields are written correctly (including a simulated browser-forced release and a simulated visibilitychange event mid-flow, and that the visibilitychange listener is genuinely torn down once the flow ends), plus tests/qr-generate-attempt-breadcrumb-20260916.cjs\'s fail-before source-reversion helper, which needed widening (a no-op-expression replace instead of a blank-line delete) to stay syntactically valid against the new nested writeAttempt call. Full regression sweep (147 files) confirms the same 23 pre-existing baseline failures, zero new regressions. Andy: next time this happens, please also tell us specifically what you did with the phone during the wait -- pressed the power button yourself, switched to another app, put it in a pocket without touching it, or just set it down and waited -- since the fix path differs a lot depending on which one it actually is, and the new breadcrumb fields should now confirm it either way regardless of what you can recall.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
