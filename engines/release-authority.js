'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-qr-wake-lock-fix-20260919a';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260919-qr-wake-lock-fix';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-19T14:20:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'CONFIRMED ROOT CAUSE, ACTUAL FIX (supersedes the diagnostic-only build shipped earlier today). Matthew Robertson\'s QR-generate modal froze on "Checking swimmer evidence... (5/5 * training_test_types) (0s)", never advancing, no error. Live Supabase edge logs for that exact minute (project cwoqjxiniuwmslltsfgi) proved every evidence job had already returned 200 in under 2.5 seconds, and no msos_bootstrap_owner / msos_publish_swimmer_payload / msos_create_swimmer_invite RPC ever reached the network afterward -- the freeze was real but entirely client-side, with nothing running to throw an error or log anything. A synchronous compute-cost check of the standards-matching work in that stretch (targetsFor/rowsByProgramme against ~4400 pathway_standards rows) benchmarked at ~90ms for 300k+ iterations -- far too fast to explain a 2+ minute freeze even generously derated for a slow phone. Rather than guess, asked Andy directly: "did your screen lock or did you switch away from the app during that wait?" -- his answer: "screen was locked, yeah". That confirms it: Android suspends a backgrounded/locked page\'s JavaScript entirely, which explains a frozen ticker (setInterval callbacks never fire), zero further network traffic, and zero thrown errors, all at once, exactly as observed -- and matches the already-tracked "phone-in-pocket/screen-off" freeze pattern. Fix: engines/swimmer-invite-bn.js\'s Generate handler now requests a screen wake lock (navigator.wakeLock.request(\'screen\')) the moment the button is tapped, keeping the screen -- and therefore the page\'s JS -- alive for the whole flow, and releases it in the handler\'s existing finally block so it releases on every exit path (success, a thrown error, or the 120s overall timeout firing). Wrapped defensively throughout: an unsupported browser, a denied lock, or any other wakeLock error can never block or break QR generation itself -- the flow behaves exactly as before if the lock is unavailable, just without the extra protection. Regression: tests/qr-generate-wake-lock-20260919.cjs -- proves the lock is requested for \'screen\' the moment Generate is tapped, released once the flow completes successfully, still released when the flow fails partway through (not just the happy path), and that a browser with no navigator.wakeLock at all still completes the flow normally; fail-before/pass-after against the exact pre-fix handler. Full regression sweep (147 files) confirms the same 23 pre-existing baseline failures recur identically before and after, zero new regressions. This should stop the specific freeze Andy has now hit three times (16th, 18th, 19th) -- but if his screen still locks for some other reason (some Android versions release a page\'s wake lock on a physical power-button press), the flow now at least hits its existing 120s timeout and surfaces a real, visible error instead of freezing silently again; the breadcrumb and checkpoints shipped earlier today stay in place to name the exact step if that ever happens.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
