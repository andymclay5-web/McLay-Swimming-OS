'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-pwa-reload-tap-window-20260918a';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260918-reload-tap-window';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-18T00:00:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'Fifth fix today. Andy, live: "I don\'t know where the reload now button is". Ground truth: there is no separate button -- the "tap to reload now" affordance IS the small "MSOS ... installed" toast banner itself (engines/pwa-update.js armReloadTap(), wired from P.notify()). Root cause: that banner was only ever visibly shown AND tappable for 2200ms -- the same blanket duration app.js\'s M.toast() uses for every throwaway toast in the whole app -- before it silently faded and its click listener was torn down. A coach had well under 2.2 seconds to notice a small banner in the corner of the screen, read it, and tap it, or it was functionally gone, indistinguishable from there being no reload affordance at all -- exactly what Andy hit live. Fix: this specific update-ready toast now overrides M.toast\'s shared 2200ms hide timer with a much longer, purpose-specific hold, P.RELOAD_TAP_HOLD_MS (15000ms in production, exported so a test can drop it for speed over the identical code path) -- the banner stays visibly shown AND tappable for the whole hold, not just its first 2.2 seconds. Every other toast in the app (unrelated to updates) is untouched -- this only re-arms the shared #toast element\'s own hide timer at the moment an update notice is shown, the same way M.toast itself already does on every call; nothing here changes the generic 2200ms default other toasts still use. Regression test: tests/pwa-update-reload-tap-window-20260918.cjs -- proves the production hold is >=10s, that a tap well past the OLD hardcoded 2200ms still reloads, that the banner correctly stops responding once its (shortened, for test speed) hold genuinely elapses, and fail-before/pass-after reproducing Andy\'s exact experience against the literal old 2200ms cutoff. tests/pwa-update-tap-reload-dedupe-20260917.cjs (the same-day 17 Sept dedupe/tap-reload fix this builds on) re-verified passing unchanged -- this fix only extends how long the tap affordance lasts, not whether or when a notice is shown. Full regression sweep confirms the same 22 pre-existing baseline test failures recur identically before and after (all meet-*, product-acceptance, session-intake, guardian, release-package, and the known styles.css hash-mismatch failure -- none touched by this fix; meet remains shelved). The same-day dosage-classification structural-default fix (v4-dosage-structural-default-20260918a) and everything before it remain in this build unchanged. No existing session-selection, target, race-pace, T400, meet, access-authority, reference-sync, roster-integrity, per-rep-effort-labeling, mod-reason-display, equipment-vocabulary, ladder-composition, rep-pattern-parsing, or dosage-classification behaviour is changed by this release.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
