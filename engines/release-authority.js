'use strict';
(function(g){
  const M=g.MSOS4;if(!M)return;
  const BUILD='v4-qr-background-enrichment-disabled-20260920e';
  M.VERSION='4';M.BUILD=BUILD;M.CORE='20260920-qr-background-enrichment-disabled';
  M.RELEASE_ATTESTATION=Object.freeze({build:BUILD,softwareReady:true,generatedAt:'2026-09-20T04:05:00+12:00',suiteDigest:'current-runtime-ci-required',packageDigest:'current-runtime-coherence',note:'A few hours after the core-payload-background-enrichment build below shipped -- and right after it reached William Callow\'s "payload:performance" step cleanly in 2.5 seconds, proving the critical-path half genuinely worked -- Andy hit a DIFFERENT, WORSE symptom on a later attempt for Matthew Robertson: "I can\'t copy the link cause it\'s frozen, cant do anything but back back." Not one modal stuck like all three earlier freezes tonight (those always left the rest of the UI responsive) -- the WHOLE PHONE, unresponsive to every tap, forcing a hard back-out. That build\'s own design comment argued that deferring the full analytical payloadFor() call via setTimeout(...,0), outside GENERATE_TIMEOUT_MS, meant it "could no longer delay or fail the access the coach already has in hand." That was right about the access staying safe, but WRONG about the phone staying usable: JavaScript has exactly one thread, so deferring WHEN a synchronous computation starts does nothing to stop it from hogging that one thread for however long it takes once it actually starts running -- a still-running background republish blocks every tap on the page exactly as badly as a blocking foreground one would, just later and disconnected from whichever button was last pressed, which is even more confusing to diagnose from the coach\'s side. safePerformance()/buildAthletePathways() (performance-pathway-ck.js, race-pace.js, wa-points.js) was read carefully earlier the same night hunting for the THIRD freeze and no unbounded loop was found -- but that is the exact same "read it, find nothing" result that missed both of the other two real bugs tonight before they were finally caught by counting real operations against real data volume instead of just reading code. Rather than ship a fourth guess while a coach\'s phone was actively locked up mid-session, the deferred background-enrichment republish is disabled entirely for now (engines/swimmer-invite-bn.js: the setTimeout block is commented out in place, with the full account in its own comment, not deleted, so it can be re-enabled once actually proven safe). Generate now publishes ONLY corePayloadFor\'s minimal shape -- identity, current session, squad sessions -- which has now been independently proven fast and reliable twice (this exact build\'s own predecessor reaching load_qr in 2.5 seconds for William Callow, and the training-view-window fix before that) -- and nothing else runs afterward that could freeze the phone. Trade-off, stated plainly rather than hidden: a swimmer\'s performance/training/test/meet portal sections will stay empty (swimmer-portal.js already renders that gracefully -- "0 events", "0m · 0 sessions", "No ... loaded yet.") until the analytical pipeline is properly bounded and this is re-enabled -- including for any swimmer who had richer data published before tonight, since every Generate republishes and overwrites that athlete\'s payload row with the minimal one. Regression: rewrote tests/qr-generate-core-then-background-enrichment-20260920.cjs (previously proved enrichment fires and succeeds; now proves the opposite -- exactly one publish call ever happens, no second call and no enrichment* breadcrumb field appear however long you wait -- plus a source-level guard reproducing the exact risk if the commented-out block is ever uncommented again without first proving payloadFor() is bounded). Reverted tests/qr-generate-concurrent-attempt-guard-20260919.cjs back to its simpler pre-enrichment shape (the enrichment-aware waiting it briefly needed no longer applies since nothing runs after \'done\' at all). Full sweep: same standing 21-failure baseline, zero new regressions, 132 passing. **Standing lesson, now proven twice in one evening**: reading this pipeline\'s code and concluding a given step is bounded has failed to catch a real bug three separate times tonight (national-benchmark, training-history, and now the background-enrichment scheduling flaw itself) -- do not re-enable or extend this pipeline\'s analytical work again without first counting real operations against data at Andy\'s actual volume, the way both of the two CONFIRMED fixes tonight were actually verified.\n\nPrior note (core-payload-background-enrichment build, now known to have its own flaw -- kept verbatim for the history it documents): Andy, live, right after the training-view-window build below shipped and unblocked "Give swimmer access" far enough to reach a THIRD analytical step this same evening: "Come on Claude this is enough, can you sort it out or do we need to find an easier way to give swimmers access" -- alongside a fresh breadcrumb for a different swimmer, William Callow, tickSeconds stuck at 0 with wakeLockHeld:true again, lastCheckpoint this time pinned to \'payload:performance\'. That is the THIRD genuinely different step (national-benchmark check, then athleteTrainingView, now safePerformance/buildAthletePathways) implicated in ONE evening inside the same payloadFor() pipeline -- a pattern, not a coincidence: this pipeline had apparently never been run end-to-end against Andy\'s real production data volume before tonight. An exhaustive static read of safePerformance()\'s entire call chain (performanceEngine.pathwaysForAthlete -> buildAthletePathways in performance-pathway-ck.js -> buildEventLadder/rowsByProgramme/chooseProgrammeRows, all tracing back to the same M.refs pathway_standards/pathway_meets tables already confirmed empty; performanceEngine.rankedEvents -> race-pace.js -> wa-points.js, also cached; app.js\'s own separate older pathway system, confirmed to read the SAME M.refs registry) found no further plausible unbounded loop -- every one either already terminates on empty reference data or is properly bounded. Rather than keep chasing individual slow steps reactively under pressure, each fix only to reach a new one, this is the structural answer to Andy\'s actual question: it stops computing ANY of the rich analytical sections (performance ladders, training accumulation, test history, meet history, shared evidence) in Generate\'s own critical path at all. engines/swimmer-invite-bn.js\'s corePayloadFor() builds ONLY identity + current session + squad sessions -- exactly what Andy asked for on 19 Sept ("I just want to give them access to their info and their squads sessions") and already fast/bounded (sessionsPartFor/candidateSessionsFor, at most 8 sessions) -- and Generate now awaits and publishes THAT immediately. The full, analytical payload (built by the unchanged payloadFor()) is computed and republished afterward via a second msos_publish_swimmer_payload call, deferred one tick (setTimeout(...,0)) so it can never delay the QR/status a coach is waiting on, and entirely outside GENERATE_TIMEOUT_MS so it can never trigger the whole-flow timeout either. msos_publish_swimmer_payload is a plain upsert keyed by athlete_id (confirmed against supabase/20260824_secure_swimmer_portal.sql: `on conflict(athlete_id) do update ...`), so this second publish safely updates the SAME record in place -- no new invite or device pairing needed, and the swimmer\'s next portal open (or a manual refresh) picks up the fuller payload automatically. swimmer-portal.js already renders every analytical section gracefully when empty (performance: "0 events"/"Tap for pathway"; training: "0m · 0 sessions"; tests/meet: "No ... loaded yet.") -- confirmed by reading its own rendering code, not assumed -- so a swimmer opening the link in the first few seconds sees a working portal with their session, just without the extra analysis until it lands moments later. The background republish\'s own outcome (ok/error/threw) is recorded on the breadcrumb as enrichmentOutcome/enrichmentMessage/enrichmentAt, and each of its sub-steps as enrichmentStep, purely via the same silent writeAttempt() mechanism -- it never touches setStatus/mark/note, so it can never overwrite the "Ready" status the coach already sees. This eliminates the entire risk category (any future slow analytical step, known or not-yet-found) from Generate\'s critical path, rather than requiring another reactive fix the next time a different step turns out to be slow on Andy\'s real data volume. Regression: new tests/qr-generate-core-then-background-enrichment-20260920.cjs (proves the critical-path publish carries corePayloadFor\'s minimal shape even with real non-empty analytical data available in the fixture; proves a second publish call follows automatically carrying the full enriched payload; proves a failed background republish is recorded as enrichmentOutcome:"error", not silently swallowed, without failing the already-succeeded attempt; fail-before/pass-after confirms reverting the one-line corePayloadFor swap makes the critical-path publish carry the full analytical payload again, reproducing the exact risk this fix removes). Updated tests/qr-generate-concurrent-attempt-guard-20260919.cjs (the winning attempt\'s own legitimate background-enrichment writes that now follow "done" are real, wanted new behaviour, not the stray-superseded-ticker bug that test exists to catch -- its assertions now wait for that enrichment to settle and confirm the core done/outcome/resolvedAt fields survive it, before proving no further write of any kind ever lands) and tests/swimmer-generate-qr-model-progress-20260913.cjs (its literal-source-match for the payload-assembly status-line wiring updated from payloadFor to corePayloadFor -- the wiring pattern itself is unchanged). Full sweep: same standing 21-failure baseline, zero new regressions, 132 passing (131 plus this build\'s own new test). Still open, and now moot rather than resolved: whether safePerformance genuinely had a fourth unbounded-computation bug for William Callow\'s specific data, or whether that breadcrumb was simply captured early during normal execution -- this fix routes around the entire computation regardless, so it no longer matters which.\n\nPrior note (training-view-window build, superseded by the above -- kept verbatim for the history it documents): Immediately after the national-benchmark-not-blocking build below shipped, Andy hit a REAL freeze again -- "Hahahah now it freezes as soon as I click generate QR" -- but this time a genuinely different one: the breadcrumb showed tickSeconds stuck at 0 for 28+ minutes with wakeLockHeld:true (tab never backgrounded), which is a true synchronous main-thread block, not an await/network hang a timeout could ever catch. lastCheckpoint pinned it to \'payload:training\' -- a step the flow had never actually reached before tonight, since every earlier attempt either hit the removed evidence-refresh freeze or the just-fixed instant national-benchmark block first. Root cause, found in architecture/training-history-core.js\'s athleteTrainingView(): it built its `records` array by mapping EVERY session ever recorded (176 on Andy\'s real device, and growing every week) through recordSession(), and recordSession() runs TWO full projection() passes per session, each calling the real per-item prescribe() (engines/coordinator.js\'s prescription()) once per set/rep item -- genuine pace/target computation, not a cheap lookup. Confirmed nothing downstream ever needed more than a trailing window: `today` only needs the current date\'s session, `week`/`month` only ever look back 7/30 days, and `upcoming` was already computed separately and cheaply (only the 4 nearest future sessions, never all of them); no caller (coach-loop-ui.js, swimmer-instant-open-cn.js, the swimmer-portal payload) reads the full `.records` array for anything else. So this function silently re-did real per-item work for months of irrelevant sessions on every single call, and on Andy\'s real data volume that was apparently enough to lock up the main thread for many minutes straight -- explaining both this freeze AND, in hindsight, plausibly contributing to some of the earlier same-night freeze theories that were never fully pinned down (buildModel/invalidate stretch) before the instant-access redesign made that particular stretch moot. Fixed: `records` is now built only from sessions within a 60-day trailing window (comfortable margin over the 30-day month summary; a session with an unparseable date is kept rather than silently dropped; future sessions are never excluded since their timestamp is always >= now). Regression: new tests/training-history-view-unbounded-recompute-20260920.cjs (counts real prescribe() calls rather than timing, which would be flaky -- proves old sessions are skipped while today/week/month/upcoming stay correct; fail-before/pass-after reproduces 320+ calls on the old unconditional source vs. a handful bounded). Full sweep: same standing 21-failure baseline, zero new regressions, 131 passing (130 plus this build\'s own new test). Still open: whether this was the ONLY thing capable of producing a true main-thread freeze in this flow, or just the first one actually reached and proven -- if a freeze recurs with a NEW lastCheckpoint value never seen before, that is real signal, not cause to assume this fix failed.\n\nPrior note (national-benchmark-not-blocking build, superseded by the above -- kept verbatim for the history it documents): Immediately after the qr-instant-access build below shipped, Andy hit a NEW hard block, instantly, every time: the freeze was gone but "Give swimmer access" for Matthew Robertson now failed at once with "Swimmer access held: No upcoming verified SCM national benchmark is linked." Andy: "This is no help to me." / "If there\'s an issue with upcoming meets we need to sort that too." Root cause: engines/swimmer-performance-ci.js\'s readinessFor() carried a third check requiring at least one event to have a linked upcoming national standard -- data that only ever comes from the pathway_standards/pathway_meets reference tables, which are only ever populated by completeEvidence(). The build below made completeEvidence() fire-and-forget in the background specifically so a slow/failing fetch could never freeze Generate again -- but that also means nothing can populate those tables before this synchronous check runs anymore, so it now fails unconditionally on any cold cache, which is Andy\'s actual chronic real-world state (pathway_standards has sat at 0 rows all session, every breadcrumb, all night). Fixed by dropping that third check entirely from readinessFor() -- confirmed safe because swimmer-portal.js already renders an event with no linked next target gracefully ("Tap for pathway" in the event summary line, "No pathway marks loaded." in the expanded detail); nothing downstream assumes every event has a linked national benchmark. Checks 1-2 (date of birth present, at least one verified race event loaded) remain hard blocks -- they are the swimmer\'s OWN evidence, never starved by an unawaited background step, so this fix does not touch them. Regression: new tests/qr-generate-national-benchmark-not-blocking-20260920.cjs (Matthew\'s real shape with an empty reference cache now passes readiness, the two remaining checks still correctly block, a source-level proof that swimmer-portal.js already degrades gracefully without a linked next target, fail-before/pass-after on the exact readinessFor() source change); updated tests/pathway-standards-reference-staleness-20260918.cjs, whose sanity/fail-before assertions previously read readinessFor().ok as a proxy for "the national-benchmark cache recovered" -- since that gate no longer exists, both now assert directly on buildModel()\'s events carrying the correct (or stale/absent) linked target instead, which is what those sub-tests actually cared about. Full sweep: same standing 21-failure baseline (20 Playwright/local-server-dependent + the flagged styles.css hash mismatch), zero new regressions, 130 passing. This does not touch why pathway_standards/pathway_meets have never once synced successfully on Andy\'s device all session -- that remains open and worth a look separately, but is no longer able to block access in the meantime.\n\nPrior note (qr-instant-access build, superseded by the above -- kept verbatim for the history it documents): A SIXTH occurrence overnight (Matthew Robertson, genuinely frozen this time -- resolvedAt/outcome both null, lastCheckpoint stuck on cache_invalidate, no connection issue per Andy) proved the refs-save-outcome diagnostics build from a few hours earlier had not found the real cause either: refs_save/t400_hydrate/cache_invalidate had all already fired, so the hang had moved past the whole evidence-refresh/local-cache-save stretch and into the handful of synchronous calls right after it (buildModel/performanceEngine.invalidate/pathwayPbCache.clear/MSOSEvidenceIndex.invalidate) -- plausibly a full-state recompute over data that has grown substantially since this flow was built (672 coach results, 152 test results, 134 captures, 72 athletes, 211MB of local storage this same night). At that point Andy said plainly: "Maybe the QR isn\'t the best way... I just want to give them access to their info and their squads sessions, this back and forth is wearing me down for 1 simple task." He\'s right, and rather than keep chasing a sixth theory for the same mechanism, this build removes that mechanism from the button\'s critical path entirely. Every freeze chased across 19 Sept -- wake lock, visibility, the concurrency guard, storage/checkpoint diagnostics, refs-save-outcome diagnostics -- traced back to one place: engines/swimmer-invite-bn.js\'s Generate handler awaiting M.swimmerPerformanceBM.prepareAthlete(), which runs a live completeEvidence() network refresh (PB board, coach results, event history, test results+types, and -- worst of all -- a full ~4400-row pathway_standards/pathway_meets refetch whenever the local copy is empty, which it always was on Andy\'s phone) before it will hand over access at all. That live call is now GONE from Generate\'s critical path. Access is built synchronously from whatever is already cached locally via readinessFor()+payloadFor() -- both were always synchronous and read-only, never the source of any freeze -- which already includes the swimmer\'s squad sessions (sessionsFor()/candidateSessionsFor() pulls every session in the swimmer\'s squad(s) across a real date window, not just "today"; this was never limited to one individual session, so no scope change was needed there). A completeEvidence() call is still kicked off immediately, but strictly fire-and-forget: never awaited, never able to block or fail Generate, its eventual outcome recorded on the breadcrumb as backgroundRefreshOutcome/backgroundRefreshAt purely so the cache keeps improving for next time and so a future investigation still has something to look at if needed. This does not explain or fix whatever the buildModel/invalidate-stretch main-thread stall actually was -- it makes that entire stretch irrelevant to this button by never putting it in the critical path to begin with. Regression: updated tests/swimmer-secure-onboarding-bn.cjs (prepareAthlete gate genuinely gone, background refresh call present), tests/swimmer-generate-qr-model-progress-20260913.cjs (live onJob wiring into Generate genuinely absent; prepareAthlete\'s own behavior, tested directly, is unchanged), tests/qr-generate-concurrent-attempt-guard-20260919.cjs / tests/qr-generate-wake-lock-20260919.cjs / tests/qr-generate-attempt-breadcrumb-20260916.cjs (their "stuck attempt" fixtures moved from a hanging prepareAthlete to a hanging msos_bootstrap_owner RPC, now the first genuinely awaited step), tests/qr-generate-refs-save-outcome-diagnostics-20260919.cjs (its invite-level assertions rewritten for the new fire-and-forget backgroundRefreshOutcome field, replacing the removed diagRefsSaveOutcome wiring), tests/swimmer-invite-double-generate-20260907.cjs (double-submit guard\'s search window widened for this build\'s added comments). Full sweep: same standing 21-failure baseline (20 Playwright/local-server-dependent + the flagged styles.css hash mismatch), zero new regressions, 129 passing.\n\nPrior note (refs-save-outcome-diagnostics build, superseded by the above -- kept verbatim for the history it documents): FIFTH+ occurrence, same day, on the storage-diagnostics build just shipped: the newest breadcrumb finally looked DIFFERENT from every prior one -- it did not hang forever, it settled after roughly 3 minutes with an honest error, caused by two real 12-second network timeouts (training_test_types -- only ~10 rows -- and pathway_standards). Both jobs timing out, including the tiny 10-row one, points at Andy\'s connection genuinely struggling in that window rather than a client-side infinite loop; this has been said to him plainly, along with a direct question about whether his connection was patchy at that moment, and whether the OLD symptom (ticker frozen at literal 0s forever, no error, ever) has still been happening lately or whether this resolved-with-an-error shape is what he has actually been seeing recently -- those may not be the same bug. The same breadcrumb\'s diagStateCounts also showed pathwayStandards and pathwayMeets both sitting at 0 rows in local memory, meaning neither had a usable cache going into that attempt -- forcing a full ~4400-row pathway_standards refetch on every single QR attempt regardless of whether that swimmer\'s check even needs it, and on a slow connection that refetch is exactly what is tipping over the 12-second job timeout. What was still not known after the storage-diagnostics build: completeEvidence()\'s post-loop M.refs.save() call (the IndexedDB write meant to persist a successful fetch for next time) is wrapped in a withTimeout()+try/catch that discards EVERY outcome identically -- clean success, silent false-return failure, timeout, and thrown error all look exactly the same from outside, nothing on screen or in the breadcrumb ever changed. That means there was no way to tell whether a pathway_standards fetch that DOES succeed on some attempt is actually being cached, or whether the save itself is quietly failing every time -- which would by itself explain the persistent 0-row local cache independent of the network timeouts. This build records that outcome (one of ok/failed/timeout/error) and its real duration on completeEvidence()\'s own return value, threaded through prepareAthlete()\'s .completion and onto the breadcrumb as diagRefsSaveOutcome/diagRefsSaveMs -- written even when the overall attempt ultimately errors, since Andy\'s real case did. This changes NOTHING about what happens on any outcome: a failed, timed-out, or throwing save still never blocks or fails the flow, exactly as before. Regression: tests/qr-generate-refs-save-outcome-diagnostics-20260919.cjs (all four outcome shapes -- ok/failed/timeout/error -- correctly classified with a real elapsed-time figure, propagation through prepareAthlete\'s completion, the breadcrumb write reproducing Andy\'s own error-outcome shape, both fail-before/pass-after against the real source for both files touched). This is explicitly NOT a fix for either the network timeouts or the true frozen-forever occurrences from earlier today -- it exists so the next breadcrumb (or Andy\'s answer about his connection that night) finally says whether the empty pathway_standards/pathway_meets cache is a fetch problem, a save problem, or both. Full regression sweep: same standing 20 Playwright/local-server-dependent baseline failures plus the one already-flagged styles.css hash mismatch, zero new regressions, plus this build\'s own new test passing.\n\nPrior note (storage-diagnostics build, superseded by the above -- kept verbatim for the history it documents): FOURTH+ occurrence, same day, on the concurrency-guard build just shipped: the concurrency fix below did NOT hold, and two facts Andy confirmed directly rule it out as the cause rather than just failing to hold -- (1) the freeze now reproduces for EVERY swimmer he has tried, not only Matthew Robertson, and (2) it survives a full app reload, which leaves no leftover in-memory attempt for a new one to collide with. So this is not the two-attempts-corrupt-each-other bug; it is something else, still unfound, that happens to land on the exact same step every time. Separately, and only because Andy went looking on his own initiative, he checked Chrome\'s own per-site storage figure for this app and found 223MB stored -- for an offline-first app whose real per-athlete evidence should be single-digit-to-low-double-digit MB, that is a strong sign something local has grown unbounded, but there is deliberately NO storage clear attempted here: Andy has marked very few sessions "Finished" while chasing this bug, so a chunk of that 223MB may be genuine, not-yet-synced coaching work (sessions, notes, possibly captured photo/video), and this app\'s own standing rule is that user data is never cleared as a repair strategy. Instead, two purely-observational, read-only diagnostics were added, neither changing behavior nor blocking the flow if unsupported: engines/swimmer-performance-ci.js\'s completeEvidence() now fires two extra non-indexed checkpoints per evidence job (immediately after that job\'s network fetch resolves, and immediately after its merge into M.state completes), each naming the real fetched-row count and the real pre/post-merge array length for that job\'s own state slot -- so a future freeze\'s breadcrumb will show whether the hang is inside the fetch/timeout wrapper despite Supabase proving success, or specifically inside the merge, and whether that array was already large before the run even started. engines/swimmer-invite-bn.js\'s Generate handler now also takes one navigator.storage.estimate() snapshot plus the in-memory length of every evidence/reference array the flow touches, written to the breadcrumb before any network activity begins, so even a freeze that never reaches a single evidence job leaves behind real MB/row-count numbers instead of a Chrome settings screen alone. Regression: tests/qr-generate-storage-diagnostics-20260919.cjs (fetched/merged checkpoint values and ordering, storage-estimate + state-count snapshot recorded and reported honestly as null when unsupported, both fail-before/pass-after against the real source). This is explicitly NOT a fix -- the real cause of the freeze is still unknown -- it exists so the next occurrence\'s breadcrumb finally narrows where the 223MB (or whatever it turns out to be) actually sits, instead of continuing to guess. Full regression sweep: same standing 20 Playwright/local-server-dependent baseline failures plus the one already-flagged styles.css hash mismatch, zero new regressions, plus this build\'s own new test passing.\n\nPrior note (concurrency-guard build, now known incomplete -- kept verbatim for the history it documents): THIRD same-day freeze for Matthew Robertson, same exact symptom, on the diagnostics build shipped earlier today -- and this time the new breadcrumb fields actively DISPROVED both standing theories: wakeLockHeld:true (the lock never let go) and no lastVisibilityState/lastVisibilityChangeAt at all (the tab never went hidden by any measure the Page Visibility API can see). So this is NOT a locked/backgrounded-tab suspension, and treating it as one for two straight builds was a real miss. Went back to live Supabase edge/postgrest logs for this exact occurrence rather than guessing a third mechanism: all 5 evidence-job network calls for Matthew genuinely completed in under 2 seconds, and unrelated background app activity (the ~25-36s coach-dashboard reconcile poll) kept firing normally throughout the reported freeze window -- ruling out both a network hang and a full engine-level JS deadlock. Re-reading the exact code path between the evidence loop finishing and the next RPC firing (engines/swimmer-performance-ci.js\'s completeEvidence/prepareAthlete/readinessFor, engines/swimmer-invite-bn.js\'s own onclick handler) found no plausible UNBOUNDED hang there -- every step is either synchronous or already timeout-protected. That contradiction (code that cannot get stuck there, breadcrumb showing it stuck there every time) pointed at how ATTEMPTS themselves were tracked rather than the evidence/RPC chain: the only guard against two "Generate" flows running at once was genBtn.disabled, scoped to ONE modal\'s ONE button. Closing the modal (Close button or the phone\'s back button) never cancelled an in-flight attempt -- its ticker, wake lock, visibility listener and any pending network/RPC calls kept running in the background for up to the full 2-minute ceiling, invisibly. Andy retrying "Give swimmer access" after a frozen-looking attempt -- exactly what three same-day occurrences on one athlete imply -- opened a brand new modal with a freshly-enabled button, with zero awareness of that still-running old attempt: both wrote to the SAME shared msos_qr_last_attempt breadcrumb key on every tick, so whichever wrote last silently overwrote the other\'s real progress. A genuinely slow-but-working retry could have its status stomped by an old, abandoned attempt re-stamping its own frozen step every second -- indistinguishable from an eternal freeze, and burning a full duplicate evidence-fetch + RPC round trip on every retry. Fixed: a module-level activeGeneration now tracks whichever attempt may touch the screen or the breadcrumb; starting a new attempt, or closing its own modal, cancels whatever it was previously pointing at, so at most one attempt is ever live. Also added lastCheckpoint/lastCheckpointAt, written directly and independently of the on-screen ticker, at every step from the evidence-job loop through the reference-cache save, the pathway model build, and each of payloadFor\'s six sub-steps -- steps that had never once appeared in three occurrences\' worth of breadcrumbs -- so if this specific gap resurfaces even once more, the next breadcrumb will finally show whether the code genuinely never reaches that point or reaches it fine while only the on-screen status is lost. Being upfront: this is a real, verified concurrency bug with a regression test proving it reproduces and is fixed (tests/qr-generate-concurrent-attempt-guard-20260919.cjs) -- but after two prior "fixes" that didn\'t hold, it is not being called a confirmed final answer to every freeze Andy has seen; the added checkpoints exist specifically so a further occurrence, if there is one, finally pins down anything this does not already explain. Also rolled into this same build, at Andy\'s request, two genuine pre-existing test failures that were unrelated to the freeze investigation: (1) v4-correct.js\'s own Guardian self-check "T400 roster starts from Here/Modified and cross-squad add appends" had Sophie Newlove seeded active:true in its fixture, when CLAUDE.md 4.33/6.9 both document her real recovered-roster status as correctly inactive -- the roster logic itself (presentRosterIds) was never wrong, the fixture had just drifted from that documented fact, so it is fixed there rather than by changing any real roster code; (2) tests/swimmer-secure-onboarding-bn.cjs had three assertions pinned to exact historical `?v=` cache-bust tags for swimmer-invite-bn.js/swimmer-performance-ci.js/swimmer-instant-open-cn.js that had simply been superseded by ordinary version bumps since (most recently by this same build) -- loosened to check each engine is loaded at all, which is what each assertion\'s own failure message actually says it is checking; a fourth assertion was still pinned to a stale pre-refactor literal (`tests:safeTests(a)`) from before this same 19 Sept checkpoint work moved that call one line earlier -- the underlying payload still genuinely carries both fields, confirmed by an equivalent shorthand-aware check. Neither was a real product regression. Full regression sweep: same 20 pre-existing Playwright/local-server-dependent baseline failures (need a dev server on 127.0.0.1:8765 not run in CI here) plus the one already-flagged styles.css release-package hash mismatch, zero new regressions -- down from 23 failures at the start of today\'s work.'});
  M.releaseAuthority={build:BUILD};
  if(M.release){M.release.attestation=()=>M.RELEASE_ATTESTATION;M.release.softwareReady=()=>M.RELEASE_ATTESTATION.softwareReady===true&&M.RELEASE_ATTESTATION.build===M.BUILD;}
})(globalThis);
