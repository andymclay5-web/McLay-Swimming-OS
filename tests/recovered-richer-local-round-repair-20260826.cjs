'use strict';
const assert=require('node:assert/strict'),path=require('node:path'),root=path.resolve(__dirname,'..');
global.window=global;global.scrollY=0;global.requestAnimationFrame=fn=>{if(typeof fn==='function')fn();return 1};
global.localStorage={getItem(){return null},setItem(){},removeItem(){}};
global.document={readyState:'loading',addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},body:{dataset:{}}};
global.location={hash:'',href:'https://repair.test/'};global.history={state:null,replaceState(){},pushState(){},back(){}};global.addEventListener=()=>{};global.removeEventListener=()=>{};
require(path.join(root,'app.js'));
require(path.join(root,'engines/parser-semantics.js'));
require(path.join(root,'engines/session-repair.js'));
const M=global.MSOS4,S=M.session,R=M.sessionRepair;
const src=`MAIN SET\n3 Rounds:\n  5 x 100 Threshold\n  200 Easy`;
const correct=M.parser.parse(src,{id:'persisted-local-richer',date:'2026-08-24',dayPart:'PM',course:'SCM'});
const malformed=M.util.clone(correct),b=malformed.blocks[0],g=b.items[0],easy=g.items.pop();b.items.push(easy);
malformed.originalPlan=correct.originalPlan;
malformed.currentSource={text:`MAIN SET\n3 Rounds:\n  5 x 100 Threshold\n200 Easy\nTOTAL 1700m`,hash:'old',updatedAt:'old'};
M.state.canonicalSessions[malformed.id]=malformed;M.state.settings.selectedSessionId=malformed.id;
M.storageEngine={readyPromise:Promise.resolve(),hydratedFromIndexedDb:false,recoveredRicherLocalState:true};
assert.equal(S.total(M.state.canonicalSessions[malformed.id]),1700,'fixture must reproduce richer-local phone bug');
assert.equal(R.shouldRepairHydratedState(),true,'richer local persisted recovery must qualify for migration');
assert.equal(R.repairStored({all:false}),1,'selected richer-local persisted session must repair');
assert.equal(S.total(M.state.canonicalSessions[malformed.id]),2100,'richer-local persisted truth must repair to 2100m');
M.storageEngine={readyPromise:Promise.resolve(),hydratedFromIndexedDb:false,recoveredRicherLocalState:false};
assert.equal(R.shouldRepairHydratedState(),false,'brand-new clean state must not be treated as persisted recovery');
console.log('RECOVERED_RICHER_LOCAL_ROUND_REPAIR_PASS');
